--- a/common/gamemodes/gamemode/payload/sv_payload.qc
+++ b/common/gamemodes/gamemode/payload/sv_payload.qc
@@ -293,6 +293,15 @@
 			GameRules_scoring_add_float2int(it, PAYLOAD_BLOCK, autocvar_g_payload_block_rate * PLC_TICK, plc_block_accum, 1);
 	});
 
+	if(time > this.pain_finished)
+	{
+		this.pain_finished = time + 0.5;
+		LOG_INFO(strcat("plcdbg t=", ftos(time), " cart=", ftos(this.plc_cart_id),
+			strcat(" s=", ftos(s_new), " v=", ftos(this.plc_speed_now), " ctrl=", ftos(ctrl)),
+			strcat(" n=", ftos(payload_n[1]), ",", ftos(payload_n[2]), ",", ftos(payload_n[3]), ",", ftos(payload_n[4])),
+			strcat(" live=", ftos(live))));
+	}
+
 	WaypointSprite_UpdateHealth(this.sprite, s_new);
 
 	if(this.plc_cart_id == 0)
