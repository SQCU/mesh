# How much of this node the mesh holds, and how much your own programs expect
# to use. Their sum may not exceed 90%: past that the node can fail to boot.
mesh_pct=25
app_pct=25

node=0          # this node's index in the mesh
peer=           # empty: wait for a peer. otherwise: the peer to reach
region=/mesh0
