# Prepared machine

## Reference baseline

The target is a measured `S >= 1.10` over fast Gemma-4-E2B B=1 decoding by overlapping useful computation across the RDMA-connected machines. The existing executor is not a preservation constraint. Source structure and compilation alone do not establish the speedup.

The unchanged `litert-lm==0.17.1` GPU benchmark measured **180.54 decode tokens/s** and **8,963.62 prefill tokens/s** on M5 Max with 1,024 prefill tokens, 256 decode tokens, 2,048 context, speculative decoding disabled, warmup and three measured iterations. The engine's `make e2b-reference` repeats that command. `output_data/e2b-reference-20260918/{benchmark.log,run.json,generation.log,generation.json}` records the benchmark and coherent generation. The package uses WebGPU over Metal. The model revision is `b3ca0d2f076785a8f4b2219ddbd2bdb99954eae1`; SHA-256 is `181938105e0eefd105961417e8da75903eacda102c4fce9ce90f50b97139a63c`.

Google's [reference results](https://developers.google.com/edge/litert-lm/overview) and [released model card](https://huggingface.co/litert-community/gemma-4-E2B-it-litert-lm) define the upstream implementation and measurement configuration. This is not a same-weight comparison with the old safetensors executor. The old local 182.378 tokens/s citation was delta-coalesced (`output_data/mesh_e3/run.sh`) and is not a baseline. The 49.146 ms resident numerical rewrite and its constructor have been deleted, including the engine target that built it. Its previous layout and instruction tables are removed with that implementation.

## Prepared operands

`H` is the client's shared-memory mapping address; `A = H + data_off` is its payload address; `P` is the system page size; `q(r,s,o)` is the physical page bound to operand `o` for rank `r` and invocation slot `s` during preparation. These integers are resolved before GPU buffer creation. Each live operand occupies its own canonical pages. Transport registration and the numerical buffer use those same pages.

| Object | Prepared address | Extent | Constructor | Runtime use |
| --- | --- | --- | --- | --- |
| <a id="M01"></a>Numerical input/output backing | `A + P*q(r,s,o)` | FFN boundary: 3,072 bytes, allocated in whole pages | `rdma/mesh-call.c:mesh_section_create`, `mesh_backing_bind` | Numerical shader reads/writes payload directly. |
| <a id="M02"></a>Receive backing | `A + P*q(r,s,o)` | Caller-specified operand extent, allocated in whole pages | `rdma/mesh-call.c:mesh_transfers_prepare` | Native receive writes payload into the final pages. |
| Native GPU and LiteRT operand handles | Prepared client M01 operand and binding addresses | One 8-byte WebGPU handle per canonical operand, one 8-byte LiteRT handle per typed view; no transport event record | Engine `tools/e2b_replay.m:main` imports each operand once, then creates LiteRT views of that same native buffer | No per-event access: compiled M06 bindings name the one canonical GPU resource. Mesh contains no numerical-provider handle wrapper. |
| GPU view of canonical backing | Same `A + P*q(r,s,o)` allocation | Page-rounded physical extent, 3,072-byte logical FP16 operand | Engine `tools/e2b_replay.m:main`, Dawn host-mapped import and `Buffer::GetMTLBuffer`, LiteRT external-buffer construction | No payload copy, map/unmap operation, allocation, or Mesh callback is added by this binding during numerical execution. |
| Reference decode graph and weights | Prepared upstream model/native objects | 2,068 original decode operators; 35 FFNs with caller-selected column intervals and original packed INT4/INT2 weights; upstream greedy selection writes the existing 4-byte token operand | Engine `tools/prepare_e2b.py:prepare`, `partition`, `greedy`, upstream `LiteRtCreateCompiledModel` | Existing LiteRT kernels execute the arithmetic, including [ARG_MAX](https://github.com/google-ai-edge/LiteRT/blob/v2.2.0/ml_drift_delegate/tflite/convert/convert_argmax.cc). At preparation, `tools/prepare_e2b.py:lower_rotations` replaces the original split-half rotation expression with ML Drift’s [existing RoPE operand interface](https://github.com/google-ai-edge/LiteRT/blob/main/ml_drift_delegate/delegate/composite/rope_parser.cc); the original expression supplies its decomposition. Geometric frequency constants and the position operand determine attributes at setup; a geometric prefix followed by zero frequencies uses the same native operation’s `proportion`, with the prefix ending on its four-channel vector boundary. The emitted native shader leaves the remaining split-half coordinates unchanged. Prefill and decode use the same lowering; their sequence/head axes are presented in the native operation’s width/height order through prepared tensor transposes. No Mesh arithmetic, callback or runtime matcher is introduced. |
| Rank contribution and consumer operands | `A + P*q(r,s,o)` for each rank contribution | FFN: 1,536 FP16 values (3,072 bytes). Vocabulary: FLOAT32 maximum and INT32 global token index (8 bytes) | `tools/prepare_e2b.py:partition`, `combine`, `vocabulary`, `append`; engine `tools/e2b_replay.m:main` | The caller may supply `--layer-placement` as a JSON map from layer number to rank shares; omitted layers use `--placement`. Only preparation reads this map and emits the existing intervals and bindings. A caller-supplied zero-width FFN interval has no contribution allocation, numerical shard, SEND cell or receive dependency. The prepared sum names only nonempty intervals. With one contributor the sum is the identity: preparation retains the original unsplit FFN and native quantization on its owner. The owner publishes the original completed layer activation at its existing M01 address, selected from the actual forward edges by the FFN output shape; this replaces the intermediate FFN broadcast with the same one-operand transaction. Other ranks bind that activation as a prepared input. `tools/prepare_e2b.py:save` removes only operations unreachable from the retained outputs once those inputs are external, following [MLIR dead-value elimination](https://mlir.llvm.org/docs/Passes/#-remove-dead-values). Original KV outputs remain roots: layer 14’s KV values are used by layers 19, 24, 29 and 34, so their producer dependencies must remain on each rank. Only now-unused query/output-attention and finishing operations disappear; there is no assumption that a whole layer is dead. Shape selection, graph slicing and bindings execute only during preparation. No sum, extra quantize/dequantize pair, extra transfer, payload copy or runtime placement decision is introduced. This applies to any rank and layer, with the same direct publication to all actual consumers; no runtime participant selection or zero-valued transfer exists. The offline `tools/mesh/solve_placement.py` caller consumes M27 publication and arrival intervals and measured shard widths for each actual backend, then emits packed-aligned layer/vocabulary shares; no solver, profile or ratio selection enters execution. FFN contributions add before original quantization and finishing operations. Vocabulary rows preserve the original projection, soft cap and ARG_MAX; M16 writes and reduces the final MAXLOC operands. The library transports both operands identically; it does not interpret their numerical operation. |

| <a id="M04"></a>Native SEND cells | `H + send_table_off + 32*((link_first + s*C + k)*T + t)`; link prefix, slot, count, invocation count `T` and invocation `t` are prepared before start | 32 bytes, `mesh_send` static assertion in `mesh.h` | `mesh_transfers_prepare`, then `mesh-flow.c:link_configure` binds the native request and queue | Dedicated TX reads each distinct ready word once and posts the already bound request; its prepared successor crosses into the next invocation. Each stream has one native QP for its whole prepared sequence. The one-stream TX entry retains that QP in a register before polling, removing the per-event QP load; independent multi-stream polling still takes the selected stream’s bound QP. This specialization concerns stream concurrency, not peer count or the numerical function. No clear, cyclic reuse or per-event address calculation remains. |
| <a id="M06"></a>Recorded native compute program | `programs + 32*i(r,s,phase)`; index fixed during graph preparation | 32 bytes, `MetalReplay` static assertion in engine `metal_recording.h`; the preparation descriptor stores command stride plus two UInt16 native binding capacities instead of a redundant resource count | `MetalRecorder.record` records upstream dispatches, pipeline states and reflected used buffer and threadgroup-memory bindings into an Apple indirect command buffer; its residency set includes the ICB, pipelines and operand allocations. Engine `metal_recording.m:30` (`MetalCreatePipeline`) constructs reflection for original and rewritten pipelines through the same preparation function; binding occupancy, accesses and scalar fields come from the compiled function before any publication argument is selected, rather than absent or inherited metadata. None of those preparation objects is traversed during execution. Per requested decode step, the recorder realizes a complete native command range with M07 directly bound at its fixed invocation offset. Original numerical pipelines and allocations are shared across ranges. Engine `metal_recording.m:208` (`MetalPromoteOperand`) compiles a pointwise remote-input consumer into its following single-workgroup reduction when both traversals cover identical indices at most once per thread; it retains one packed vector per thread rather than rematerializing the producer at each read. The generated entry retains the original SEND/receive prefix; its pointwise function executes only on the first traversal, and the second traversal reuses that exact packed result. The intermediate dispatch, its global store and both reduction loads disappear. This is [LLVM Linalg producer/consumer fusion and promotion](https://mlir.llvm.org/docs/Dialects/Linalg/); selection and binding are preparation-only. The promoted `uint2` has an 8-byte MSL static assertion; its address is thread-local, with no global metadata load. Original arithmetic, packing, reduction barriers and cancellation remain. Engine `main` applies Apple’s [native range optimizer](https://developer.apple.com/documentation/metal/mtlblitcommandencoder/optimizeindirectcommandbuffer(_:range:)) to the complete prefill/decode ranges during preparation; each execution uses that entire optimized range | A native encoder executes the prepared command range. Metal 4 [requires residency for indirectly referenced pipeline states](https://developer.apple.com/documentation/metal/mtl4computecommandencoder/executecommands(buffer:indirectbuffer:)). All allocations remain owned after LiteRT executors are destroyed; recording and LiteRT invocation are absent from replay. `MetalReplayInterleave` places the already-recorded M13 boundary commands between the M06 bodies using Apple’s [native indirect-command copy](https://developer.apple.com/documentation/metal/mtlblitcommandencoder/copyindirectcommandbuffer(_:sourcerange:destination:destinationindex:)) during preparation. Bodies and boundary commands retain their exact bindings. The boundary recorder captures its explicit dependency before encoding the first boundary command, as Apple requires for `setBarrier`; the native copies preserve that dependency and the prepared barrier before each subsequent body. No already-encoded command is patched after copying. The combined residency owns the original numerical allocations and the resulting command buffer. For a function with no boundary commands the existing prepared body sequence is used directly. Ordinary submission executes the complete interval in ranges split only at the warmup timestamp; per-invocation Metal 4 execution wrappers and dispatch-wide barrier commands are deleted. Diagnostic splits remain fixed timestamp positions, with no command reconstruction or host interaction during execution. |
| <a id="M07"></a>Native receive-completion operands | `A + P*q(r,o,completion) + 32*(s*T+t)`; operand `o`, concurrency slot `s`, requested invocation `t` and count `T` fixed before start | Thirty-two bytes per slot and requested invocation: independent 64-bit completion and cancellation fields occupy the first 16 bytes, followed by padding; a 32-byte aligned mesh_input_status static assertion fixes the invocation stride; 32-byte `mesh_metal_input` and 16-byte `mesh_metal_transport`, static assertions in `mesh-metal.h` | `mesh_metal_receive_prepare` allocates distinct canonical completion words and resolves their native relative addresses; engine `MetalRecorder.receive` binds them to every actual reading dispatch during preparation | Consumer read: 0 loads to find the bound word. CQ stores only the completion field; cold cancellation stores only its adjacent field. Numerical observers load both in one coherent 128-bit vector read without clearing either. During M06 recording, an existing native dependency barrier completes all preceding commands. For incoming operands read before that barrier, `MetalComputeRecorder.recordDispatch` removes the binding from its preparation-only pending-input map; subsequent readers need no repeated observation, system fence, cancellation scratch or workgroup handoff. Independent readers before that existing barrier keep their observers. This follows Apple’s [ICB barrier ordering](https://developer.apple.com/documentation/metal/mtlindirectcomputecommand/setbarrier()); it inserts no barrier or runtime availability state. Each prepared invocation still contains its first observers with its own M07 bindings. Explicit cancellation is reported through the first abandoned observer’s existing M12 word. No generation, reset race, extra receive dispatch or runtime address selection. |
| <a id="M17"></a>GPU SEND-cell view | View of the canonical SEND table allocation; invocation `t` binds the native view at byte offset `32*t`, with each destination at its prepared constant offset `32*(link_first+s*C+k)*T` | View extent is the page-rounded sum of all prepared SEND cells times `T`; M04 cell 32 bytes, asserted in `mesh.h` | `mesh_transfers_prepare` allocates the canonical table and realizes per-link bases; `mesh_metal_transport_create` makes its native view; `mesh_metal_publication_prepare` resolves constant M10 offsets; `MetalRecorder.record` binds each invocation offset before replay | Value-ready: fused consumers use their already bound M07 pointer and prepared constant GPU-address displacements to M04; final-store producers use the M28 binding and the same prepared displacement. Both share the 32-byte invocation stride. Single-thread producers bind M17 directly. None reads M10. Standalone publication reads its prepared M10 record. Invocation offsets are native bindings prepared before submission, never a shader counter or runtime lookup. |
| <a id="M28"></a>Producer output completion | `A + P*q(r,s,l,output_completion) + 32*t`; output, invocation count and destinations fixed before submission | 32-byte aligned `mesh_output_status`, `_Static_assert` in `mesh-metal.h`; one remaining-byte word and padding | `tools/prepare_e2b.py` records each FFN collective’s contributor count in its setup route. Engine `tools/e2b_replay.m` requests this storage only for multi-contributor reductions; singleton broadcasts have no output-completion word. `mesh-metal.m:93` initializes each requested word to the logical output extent in canonical pages; engine `metal_recording.m:712` selects the complete single-write projection, binds its 32-byte stride and specializes M04 displacement/argument pairs into `MetalCoherentStores` (`metal_recording.m:101`) | Each original output store performs its existing system-coherent store/fence. Preparation resolves the writer set from the complete native output grid: only local y=0 writes, x ranges over the fixed workgroup width, and all writers fit one SIMD group. The x=0 lane subtracts the literal workgroup width times eight bytes from this directly bound word. Apple's [SIMD execution](https://developer.apple.com/videos/play/tech-talks/10858/) keeps those writers in lockstep through their existing store/fence; no runtime SIMD sum or active-lane election reconstructs the prepared writer set. Exactly the final writer group stores M04; all others return without polling or a new barrier. The native 4-by-64 projection has four active output lanes in one SIMD group: 96 atomic updates cover its 3 KiB output; the two-by-128 INT2 projection has 192 updates. The pipeline's execution width and the complete output extent are checked only during preparation. This is the completion-aggregation principle in NVIDIA's [threadFenceReduction](https://raw.githubusercontent.com/NVIDIA/cuda-samples/master/cpp/2_Concepts_and_Techniques/threadFenceReduction/threadFenceReduction_kernel.cuh), applied to output stores rather than a numerical reduction. Preparation limits this lowering to the native producer’s single, non-overlapping, complete output write, independent of output variable names. The collective’s contributor count is consumed only at preparation and never appears in native SEND/RECV or GPU execution. Other source geometries retain the native producer dependency. No counter reuse, reset, generation, occupancy test, receive change or host aggregation. Additional atomic work must be compared with the removed dispatch-to-publication delay; no speedup is assumed. | 0 |
| <a id="M08"></a>Native stream and receive records | `queues + 64*(q*F+s)`; receive record `receive + 128*(t*C+f)`, with prepared frame count `C`, frame index `f` and invocation `t`; one terminal record follows the finite sequence | `mesh_queue` 64 bytes and `prepared_receive` 128 bytes; next native request, QP, destination and value occupy its aligned 32-byte line at +64; static assertions | `verbs_up` requests the device-reported bounded native queue capacity and records the actual capacity returned by the QP; `link_configure` constructs every native request, destination and refill binding, and preposts the maximal whole-invocation prefix that fits every configured queue. `link_run` creates two progress threads | For one prepared receive queue, its hardware completion order is invocation then frame, so the drainer advances its register-held record address by the literal 128-byte stride; the successor-pointer field and load are deleted. The terminal record repeats the last binding while the drainer handles remaining SEND completions until teardown. Multiple independent receive queues retain native `wr_id`, because their ordering is not fixed. If the entire finite receive sequence fits, no receive repost is compiled into that drainer; otherwise each completion immediately submits the native request at its precomputed window displacement. The final window has no further native requests: its prepared refill pointer is null and no post is made. This is a finite-action test, not a capacity test, occupancy counter or credit wait. For unordered refill, the next-request/QP pair is loaded with one native AArch64 `ldp` from the asserted +64/+72 fields, including when the request is absent; LLVM cannot split that pair around the finite-action test. Ordered/unordered and refill/no-refill are selected once at thread entry from prepared topology/capacity. [Hilland et al. section 8.2.2](https://www.rdmaconsortium.org/home/draft-hilland-iwarp-verbs-v1.0-RDMAC.pdf) supplies within-RQ ordering; [Dotan Barak’s QP contract](https://raw.githubusercontent.com/linux-rdma/rdma-core/master/libibverbs/man/ibv_create_qp.3) supplies realized queue capacity, and [post_recv](https://raw.githubusercontent.com/linux-rdma/rdma-core/master/libibverbs/man/ibv_post_recv.3) supplies preposted requests. All payload-reuse dependencies remain in the prepared numerical graph. QP and polling-thread counts stay independent of requested steps. Only the final SEND request of each stream pass is signaled; its immutable template repeats. One shared native CQ drains successful SEND completions for reclamation and publishes RECV completions through their prebound destinations. Status precedes opcode inspection, using one 64-bit CQ load. There is no runtime cadence counter, queue-credit check, extra completion wait or delayed SEND. |
| <a id="M09"></a>Registered payload regions | `wire_base + i*floor(max_mr_size/(P*block))*(P*block)`; device capacity and `i` resolved at start | Native MR handles; each SGE is 16 bytes, asserted in `mesh-flow.c` | `rdma/mesh-verbs.h:183` binds device-sized regions and precomputes each SGE's lkey | No registration work or region lookup occurs on a transfer. |
| <a id="M10"></a>GPU publication operands | `A + P*q(r,s,publication) + 32*p`; each destination is a constant byte offset of M04 within M17 | 32 bytes per `prepared_publication`, static assertion in `mesh.h` | `mesh_metal_publication_prepare` computes the prepared native cell offsets once | All publication paths read records only during preparation and compile destination/argument literals into M06; the cache key includes the pairs and the SEND resource stays resident. M28 projection writers publish directly after their final system-coherent output store; no following command publishes that operand. Single-thread producers append the same literals after their output. For other source geometries, preparation retains the native producer dependency and folds publication into an eligible following consumer, or emits a standalone command. A fused consumer publication fence also covers its first completion observation; receive-only entries and later inputs keep their initial fences, and failed observations fence before retry. No per-event destination lookup, payload reread/rewrite, extent load, host aggregation or runtime lowering selection. Engine `MetalRecorder.publish` compiles the same literal M04 destination/value stores for an unfused publication as `MetalPublicationStores` emits for fused entries. The prepared records are CPU compilation input only, with no GPU binding or residency. The final M06 command is one thread writing every declared destination; its fanout and displacements are compiled at preparation. `mesh_metal_transport_create` creates only the M17 view and M12 cancellation backing: the generic record-reading GPU pipeline is deleted. Original producer dependency and system fences remain; no runtime record load, lane-indexed destination lookup or numerical change is introduced. |
| <a id="M11"></a>Native CQ output | `CQ_base + 48*peer` | 48-byte `ibv_wc`; adjacent 4-byte status/opcode at +8/+12, static assertions in `mesh-flow.c` | `mesh-flow.c:571` allocates one output before starting the drainer; `mesh-verbs.h:verbs_up` binds both QP directions to one CQ using the device-reported bounded capacity | One native CQ poll supplies the next hardware completion. The drainer loads status and opcode together; successful SEND completions return immediately to polling, without advancing the register-held receive binding. Successful RECV completions store through that binding; only submissions exceeding the prepared receive window also post their already-bound refill request. Following [JACCL’s single-CQ connection](https://github.com/ml-explore/mlx/blob/main/mlx/distributed/jaccl/lib/jaccl/rdma.h), no separate SEND-CQ spinner, notification, timer or host submission is retained. These are two spinning threads per active link, independent of invocation count. |
| <a id="M12"></a>Invocation cancellation operand and prepared input ranges | Header at `A + P*q(r,stop)`; range `header + 32 + 32*i`; each range names M07 by its prepared mapping offset and word count | 32-byte `mesh_cancellation` header and 32-byte `mesh_cancel_range`, both statically asserted; stream-header offset remains +16 | `mesh_metal_transport_create` allocates capacity from prepared receive operands; `mesh_metal_receive_prepare` records each M07 extent before execution | Explicit interruption clears M26 configured ownership and wakes its existing lifecycle waiter. Control retirement shuts down the native control connection before calling `mesh_cancel` to release absent-input observers; startup returns cancellation before dispatch if the request preceded transport start. Native/control-link failures use the same close ordering. This traversal runs only on shutdown, never on a successful crossing or an ordinary GPU poll. Completion and cancellation use disjoint 64-bit stores, so neither can erase the other. A completed operand still wins over cancellation, as before. The GPU writes abandonment only when it exits with an input absent. |
| <a id="M24"></a>Native cable-loss notification | `links + p*sizeof(mesh_link) + offsetof(mesh_link,network_event)`; notification socket belongs to the same prepared link | 48-byte `mesh_network_event`, statically asserted against `KEV_MSG_HEADER_SIZE + sizeof(net_event_data)` | `mesh-flow.c:link_run` subscribes to Apple's `KEV_NETWORK_CLASS/KEV_DL_SUBCLASS` before pairing and registers the socket with its existing kqueue; `link_close` closes it | Control event only: `KEV_DL_LINK_OFF` or `KEV_DL_IF_DETACHED` for the configured native device invokes M12 cancellation and ordinary QP/CQ teardown. Successful TX/RX and numerical consumption never read this object: 0 crossing loads. No new thread, timer, heartbeat, transport retry or interface reconfiguration. Apple [KernelEventMonitor](https://github.com/apple-oss-distributions/configd/blob/main/Plugins/KernelEventMonitor/eventmon.c) supplies the socket/filter/event contract; the installed verbs SDK exposes no `ibv_get_async_event`. Physical cable-loss recovery remains unmeasured. |
| <a id="M13"></a>Prepared decode boundary operands | State `A + P*q(r,state) + 32*i`; emitted token `A + P*q(r,tokens) + 4*i`; position and parameters use M01; mask word offset `4*floor(min(i+1,context-1)/4)` | 32-byte `DecodeState`, `_Static_assert`; 4-byte token per requested step | Engine `tools/e2b_replay.m:main` prepares state values, canonical backing and one bound copy command per step before submission | After the numerical step, `decode_advance` copies the prepared parameters, position and mask word and stores the output token at its fixed destination. One state record read, one token read; all target bindings are prepared. The submitted command sequence repeats M06 and the corresponding boundary copy. No host wait, parameter store or command construction occurs between steps. |
| <a id="M14"></a>Reference prefill operands | `A + P*q(r,prefill,o)`; position element `base + 16*i`; masks `base + i*context + j`; KV outputs bind the same M01 pages used by decode | 1,024 INT32 prompt tokens; 1,024 native padded positions; 1,024-by-context boolean mask; original native embedding, PLE and KV extents | `tools/prepare_e2b.py:prepare` extracts the original prefill signature; `tools/e2b_replay.m:main` binds it before M06 decode in M19's compute sequence | The original 1,107-operator prefill graph populates KV directly. M19 encodes native dispatch dependencies at the prefill/decode and numerical/boundary-copy cuts; residency sets alone do not order memory accesses. No separate prefill command buffer, host wait, fence object, output copy or runtime rebinding. |

| <a id="M19"></a>Native interval submission | `submission + {0,8,16,24}`: allocator, command buffer, queue, counter heap handles, all realized before `mesh_transfers_start` | 32-byte `Submission`, `_Static_assert` in engine `tools/e2b_replay.m`; native allocations have provider-owned extents | Engine `tools/e2b_replay.m:main` records the complete prefill/decode sequence into a Metal 4 command buffer | One host submission, outside both crossings. Zero per-event loads of this record. Explicit native range dependencies replace the Metal 3 serial encoder's implicit range barriers; no host step work. |
| <a id="M20"></a>Decode interval timestamps | Native heap entries 0 and 1, fixed before submission; resolved entries `samples + 8*i` | Two 8-byte `MTL4TimestampHeapEntry` values; `_Static_assert`; provider-owned opaque heap | Engine `tools/e2b_replay.m:main` binds [precise native timestamp commands](https://developer.apple.com/documentation/metal/mtl4computecommandencoder/writetimestamp(granularity:counterheap:index:)) after the first completed decode step and after the last step (a one-step invocation measures that one step after prefill) | Endpoint measurement only, not a layer/step operation. The first collective step absorbs unequal prefill durations; `measured_steps` explicitly excludes it from the interval. All requested tokens remain in the result. Native heap ticks are divided by the device's [queryTimestampFrequency](https://developer.apple.com/documentation/metal/mtldevice/querytimestampfrequency()), realized before submission (24,000,000 Hz on M5 Max); the Metal 3 `sampleTimestamps` clock conversion does not apply. No overlapping-interval subtraction. |
| <a id="M22"></a>Crossing measurement operands | Diagnostic heap entries `2 + 4*l + {0,1,2,3}` and preparation-only cut `cuts + 32*l`; step `warmup+l` fixed before submission | 32-byte `MetalCrossing` (`numerical`, `publication`, `consumption`, `step`), `_Static_assert` in engine; four 8-byte native timestamp entries per sampled crossing, plus two adjacent timestamp entries after the measured decode interval; two 32-byte `ClockPair` records with raw CPU brackets and native CPU/GPU samples, statically asserted | `MetalComputeRecorder.recordDispatch` captures native command offsets in `MetalRecorder.crossings` while recording; diagnostic preparation emits their native workgroup geometry; `tools/e2b_replay.m:main` encodes samples before the numerical region, before and after publication, and after the first numerical consumer when `TRACE_CROSSINGS` is supplied at preparation | No production-path use. The consumer endpoint follows its first numerical dispatch, since no standalone observation dispatch exists. For publication appended to a producer, the publication interval begins before that producer and includes its numerical work; it is not a pure transport interval. One crossing is sampled per step; an integer `TRACE_CROSSINGS` value repeats the prepared sweep that many times within the requested step count, recording the actual Metal device name and rank. Repeated 32-byte cuts and all timestamp indices exist before submission; native precise timestamps perturb that sampled crossing, so these are diagnostic upper bounds, not H7 pass evidence. When publication is standalone, its interval and the following first-consumer interval are separate. A fused publication/consumer is reported as one combined interval and explicitly marked fused; its internal event timing is not inferred. Otherwise, the latter includes remote producer imbalance, transport and the first consumer dispatch and is not pure wire latency. Raw GPU ticks and frequency remain available. Before submission and after retirement, [sampleTimestamps:gpuTimestamp:](https://developer.apple.com/documentation/metal/mtldevice/sampletimestamps%3Agputimestamp%3A) records paired clocks inside `CLOCK_UPTIME_RAW` brackets. The samples, brackets and M20 raw endpoints are emitted only diagnostically. Postprocessing checks scale, bracketing and consistency with the Metal 4 counter range before aligning local M27 events; it makes no cross-node clock alignment or wire-time inference. Two adjacent precise timestamp commands after M20 measure a no-work sampling interval; it is reported separately, not subtracted as an assumed constant from crossing measurements. Diagnostic replay retains complete command bindings without native range optimization because it executes subranges. Ordinary D2 replay retains its complete optimized range and two M20 endpoint samples. |
| <a id="M21"></a>Final host completion observation | `completion + {0,8}`: native semaphore and retained commit feedback | 16-byte `Completion`, `_Static_assert` in engine `tools/e2b_replay.m` | Engine `tools/e2b_replay.m:main` installs the one native commit feedback handler before submission | The measurement caller observes retirement/error once after the whole invocation. Neither a per-step handler nor a dependency of a producer, SEND, RECV or numerical consumer. |

M07 uses the MLX authors' [system-coherent Metal accesses and system-scope fences](https://github.com/ml-explore/mlx/blob/main/mlx/backend/metal/kernels/fence.metal). Publication is recorded at setup. M07 observation is compiled into the original numerical consumer entry point using [Dawn’s native MSL compilation boundary](https://github.com/google/dawn/blob/main/src/dawn/native/metal/ShaderModuleMTL.mm). Pipeline reflection identifies actual operand readers during preparation; each receives direct completion and cancellation bindings. Original numerical source follows that entry prefix, with the preparation-only reduction lowering described below. The compiler descriptions and binding searches are absent from execution. An explicit stop operand supports interrupted measurements. Each rank records only its assigned FFN, publishes its contribution to every prepared consumer, and consumes remote contributions in their final canonical backing. These operations still execute as commands inside the step replay; their crossing latency and across-step residency are not established by this construction.

The [released cache-update parser](https://github.com/google-ai-edge/LiteRT/blob/v2.2.0/ml_drift_delegate/delegate/composite/add_values_to_cache_parser.cc) uses three of its seven inputs and writes native byte-format KV outputs in place. Its other inputs support the CPU decomposition and are not read by GPU execution. The original signature still supplies them during setup, using host tensor handles over canonical pages. No floating KV conversion is inserted. Runtime replay uses the native cache output bindings directly. The engine caller aliases corresponding KV input/output names from the upstream signature at setup.

The external WebGPU binding imports the actual M01 pages through Dawn's host-mapped buffer extension. Each canonical operand has one WebGPU buffer, shared by all LiteRT views. Native numerical commands use that object's Metal buffer through the pinned provider's [Buffer::GetMTLBuffer](https://dawn.googlesource.com/dawn/+/refs/heads/main/src/dawn/native/metal/BufferMTL.mm) accessor. That accessor is setup-only and is not a stable public Dawn ABI. Separate numerical buffer objects over the same pages are deleted. The reference library supplies the numerical kernels. The deleted native Metal delegate, its SDK and its queue-residency bookkeeping are not another execution path.

M06 device preparation uses the reference provider's supported subgroup-matrix feature and native adapter limits, with `disable_robustness` and `disable_polyfills_on_integer_div_and_mod` passed to Dawn before compilation. The released `ml_drift::webgpu::Environment::Initialize(InitParams const&)` requests matrix feature `0x50034` at `0xca6450`, constructs these toggles at `0xca6598` and `0xca65f4`, and queries adapter limits at `0xca66dc`; engine `output_data/e2b-metal-replay-20260918/reference-device-disassembly.txt` records those instructions. Dawn's [Metal compiler lowering](https://dawn.googlesource.com/dawn/+/refs/heads/main/src/dawn/native/metal/ShaderModuleMTL.mm) consumes the flags before generating MSL. Engine `tools/e2b_replay.m:main` constructs the device once, enables experimental API access before adapter discovery, and installs the native error callback. No capability query or compiler option exists on the crossing path.

`tools/prepare_e2b.py --layers LO:HI` selects an original contiguous layer interval before partitioning; this inspection path retains those layers’ attention, FFN, norms, residual and PLE, with original boundary operands. M01 initializes floating boundary inputs to a fixed nonzero value at preparation and initializes cache/control inputs explicitly. Its M06 has no embeddings, prefill, vocabulary, sampler or decode-advance commands. Repetition uses the same prepared collective bindings and unique receive-completion offsets; no timing code enters a numerical function. The ordinary graph remains available, but execution is restricted to two layers until the collective algorithm is explained and corrected. `tools/prepare_e2b.py` constructs one M06 numerical graph per rank, retaining local partials as graph outputs and remote partials as graph inputs. Its setup scope maps each composed fragment’s public input/output identity to the existing tensor index; internal tensor names are not used as value identities. Original KV outputs remain explicit outputs for the next step. Ordered `sends` operands specify destinations and publication order, which M04/M08 bind before execution without sorting tensor names. M06 attaches publication to the native writer, so composing the graph introduces no end-of-function publication delay. The manifest’s `partitions` retain the caller’s layer widths and rank intervals for the offline solver; they are not execution callbacks. With no requested collective cuts, the original decode graph and greedy output form one graph. M01 contains model inputs, KV state and the 35 FFN partials; M04/M08 retain the 35 FFN and one vocabulary publications. Composition, upstream compilation and bindings are complete before invocation. Existing `DUMP_SHADERS` preparation output identifies each publication writer and its remote operand reads using native buffer labels, so a graph fusion that would wait for its own exchange is visible before submission. The same output identifies each removed publication and its replacement native function, and lists each retained command’s native source, grid, dependency barrier and directly bound operand accesses. Labels and inspection execute only during preparation.

Vocabulary selection follows the MPI Forum's [MAXLOC](https://www.mpi-forum.org/docs/mpi-5.0/mpi50-report/node135.htm): largest value, with the smallest global token index on a tie. Contiguous increasing row intervals make rank order agree with index order. M16 writes a FLOAT32 value and INT32 global index directly into the final eight-byte operand. The existing reference ARG_MAX supplies the local index; native gather and scalar conversion consume its FP16 logits. Consumer comparison reads each pair through its prepared binding, choosing the lower index on equal values. The transport treats the pair as an ordinary operand. No BITCAST, concatenation, slicing or intermediate candidate array is retained.

## Executed connection

`output_data/e2b-native-metal-20260918/{prepare-run.json,execute-run.json,execute.log}` records the released native Metal backend preparing and executing the original first FFN on canonical Mesh pages. Both operands require 3,072 bytes; all 1,536 output values are finite, 1,524 nonzero. The client and isolated bridge exit 0. Planned bytes: 67,960,832 arena + 67,108,864 model/upload + 536,870,912 compiler = 671,940,608. The measurement explicitly waits for native queue completion before reading output; production publication and replay are not implemented by the page binding. `graph-full.json` and `graph-11-5.json` record complete source-operator and dependency coverage, not a numerical or throughput result.

`output_data/e2b-rdma-preparation-20260918/operand-layers.json` and `operand-layer-{0,15,34}.log` record execution of reference FFNs on real canonical Mesh pages through LiteRT's WebGPU backend. All three produced 1,536 finite FP16 outputs; the native clients and isolated bridges exited 0. Both 6,144-column INT4 and 12,288-column INT2 FFNs were exercised. The arena was 76,922,880 bytes; model/upload allowance was 64 MiB and compiler allowance 512 MiB. No network device or persistent dispatch was opened. Elapsed process times include setup and are not throughput measurements.

Native queue completion was observed explicitly by the measurement client before it read the shared output. External-buffer execution supplied no LiteRT tensor completion event; returning from `LiteRtRunCompiledModel` did not establish CPU-visible completion. The page-binding library contains no invocation, wait, completion callback or scheduler. No crossing latency or `S >= 1.10` result exists yet.

`output_data/e2b-partition-20260918/{runs.json,combine-runs.json,comparison.json,cost.json}` records both rank contributions and their native combination for layers 0, 15 and 34. All graphs reported full non-CPU acceleration and returned finite values. Different floating accumulation order changes rounding at quantization boundaries: 10, 23 and 9 of 1,536 outputs differed from the respective unsplit graph; no bitwise-equivalence claim is made. The source-refactored generator reproduced all 105 exercised-format model artifacts byte-for-byte. These isolated repeated-layer timings are not decode timings: weights remain cache-hot, and a separately invoked combination cost 21.8–33.1 microseconds. The emitted consumer now incorporates that addition into the original finishing graph; no separate combination artifact is referenced by the manifest.

`output_data/e2b-consumer-20260918/{runs.json,comparison.json,three-ranks.json}` records native GPU execution of the composed consumers for layers 0, 15 and 34 and a three-contribution consumer. All 1,536 values in each of the three compared layers equal the original finishing graph fed by the previous separate combination. The two- and three-rank configurations prepare all 35 consumers. These are isolated graph executions, not a distributed decode speedup.

## Deleted executor

| Source | Deletion | Remaining execution role |
| --- | --- | --- |
| `rdma/mesh-call.c`, `mesh-call.h` | Numerical worker threads, arrival scans, pending-count gates, callback submission, operand reconstruction, submission arrays and their API | Canonical operand allocation and transport preparation; invocation-slot count is supplied directly to transport preparation. |
| `swift/Mesh.swift` | Entire numerical executor and its CPU, Metal, Core ML and resident callback paths | None; the reference numerical implementation is external. |
| `rdma/mesh.h`, `mesh-dataflow.c`, `mesh-flow.c` | Numerical arrival/completion records, consumer-target branch and eight-worker queue reservations | Native SEND records and transport completion handling. Shared-memory ABI is 88. |
| `rdma/Makefile` | Swift executor build, assembly target and obsolete example targets | Native transport and shared-page library builds. |

`output_data/e2b-executor-removal-20260918/{plan-v88.json,run-v88.json,consumer-v88.log}` records the consumer running after the executor and queue-layout deletion, including independent `mesh_transfers_prepare(context, 1)`. Output is unchanged; client and isolated bridge both exit 0. Native C assembly builds pass with `-Wall -Wextra -Werror`. This check does not execute a cross-node transfer or establish the differential decode budget.

| <a id="M15"></a>Original index-expression operands | M01 pages of the original integer inputs; their prepared native bindings are in M06 | Original CAST, RESHAPE and CONCATENATION expressions rooted only in integer inputs and constants; no event record | Engine `tools/prepare_e2b.py:extract` retains original source-graph/tensor identity for these expressions only during preparation; `append` binds repeated occurrences to their first value and removes the copied operators before native compilation | One evaluation of each original index expression remains inside the composed graph, following [compiler common-subexpression elimination](https://openxla.org/xla/hlo_to_thunks). These identities are absent from the serialized model and execution. No host read, packing conversion, callback or repeated index dispatch is introduced between fragments. |

## Native replay

M06's FFN partition emits an unquantized down-projection contribution; the original output quantization remains after the sum. The proposed deletion of its input DEQUANTIZE was rejected before GPU dispatch: `quantized-input-record/rank0-client.log` records the released provider's `fully_connected.cc:296` requiring an integer output for an INT8 input, despite the broader input types listed in Google's [delegate support](https://github.com/google-ai-edge/LiteRT/blob/main/ml_drift_delegate/tflite/support/support_fully_connected.cc). Engine `2670dad` restores the operand conversion required by this frontend. No mixed-output bypass, arithmetic replacement or second execution path remains. This result does not imply that the conversion survives downstream fusion as a distinct dispatch.

M06 specializes the upstream `Scalars` integer and floating vectors after setup uploads complete and before ICB construction. Native pipeline reflection supplies each `U` binding, vector type and member offset. One setup-only [Metal blit](https://developer.apple.com/documentation/metal/mtlblitcommandencoder/copy(from:sourceoffset:to:destinationoffset:size:)) reads the private configuration blocks; `MetalSpecializeScalars` substitutes each vector's exact bit pattern with a typed constant in the existing shader and caches each resulting pipeline by its original pipeline and constant bytes. The readback buffer and compiler descriptions are destroyed before invocation. Tokens, positions, masks, KV state, tensor weights and scale arrays, and received partials remain operands. Fixed scalar coefficients in the immutable `Scalars` configuration are specialized using bit casts, without decimal conversion. The resulting pipeline is the M06 object, with no extra runtime record, binding lookup or allocation. This applies Apple's [pipeline specialization](https://developer.apple.com/documentation/metal/using-function-specialization-to-build-pipeline-variants) during configuration; [Dawn private-buffer uploads](https://github.com/google/dawn/blob/main/src/dawn/native/metal/BufferMTL.mm) must finish before their constants can be read.

M06's `MetalPackedWeights` replaces the native two-bit byte-unpacking expression with unsigned shifts, masks and exact integer-to-float conversions during recording. For byte `b`, `4*(b/4-floor(b/4)) = b & 3`; the following digits are `(b >> 2*j) & 3`. The full original expression must match before lowering. Weight bytes, scale/bias operations, multiply-accumulates and output presentation remain identical. [MLX quantized dot products](https://github.com/ml-explore/mlx/blob/main/mlx/backend/metal/kernels/quantized.h) use bit masks rather than floating remainder extraction. No unpacked weight allocation or runtime compiler path is introduced.

M06 realizes Dawn's [`disable_workgroup_init`](https://github.com/google/dawn/blob/main/src/dawn/native/Toggles.cpp) for this upstream tensor program before compilation. The emitted projection, norm, tiled multiplication, softmax and MAXLOC kernels assign their scratch entries before the first dependent read; reduction identities and out-of-range values are set by those kernels themselves. The WebGPU-inserted zero stores and entry barrier therefore supply no operand. Their removal changes neither tensor initialization nor inter-dispatch dependencies. `disable_symbol_renaming` preserves upstream shader names for source inspection; both options affect compilation only.

M06's `MetalProjectionReduction` lowers the native projection's exact descending `float4` sum tree during recording. The recorded threadgroup dimensions and pipeline `threadExecutionWidth` determine the SIMD rows at preparation. Strides crossing SIMD groups retain the original shared-memory exchange; smaller strides use `simd_shuffle_down` with the original addition order, as in Apple's [MLX quantized matvec](https://github.com/ml-explore/mlx/blob/main/mlx/backend/metal/kernels/quantized.h). The full source expression must match before lowering; other numerical programs are unchanged. The prepared pipeline replaces the original in M06. No runtime matcher, pipeline selection, new operand allocation or collective work is introduced. `MetalNormalizationReduction` lowers the upstream normalization sum over a complete one-dimensional workgroup to [MLX RMS normalization](https://github.com/ml-explore/mlx/blob/main/mlx/backend/metal/kernels/rms_norm.metal)'s two-level SIMD reduction: one sum per SIMD group, a shared exchange, a SIMD sum of those group results, and the final shared broadcast. Exactly two threadgroup barriers remain when multiple SIMD groups participate; no barrier is needed for one group. Inactive reduction lanes supply zero directly, so no shared-memory zeroing pass is needed. This changes floating addition grouping under the native program's existing relaxed math mode; it does not change the normalization formula, operand coverage or output bindings. There is no new dispatch.

`metal_recording.h` exposes a 32-byte, 32-byte-aligned `MetalReplay`. `MetalRecorder.record` accepts the upstream encoder function and records its pipelines, grids and buffer bindings into an Apple indirect command buffer. The recorder captures only preparation. Replay uses its prepared residency set and `executeCommandsInBuffer`; it does not call LiteRT or rebuild bindings. M06 barrier positions are compiled from Metal's [pipeline reflection bindings](https://developer.apple.com/documentation/metal/mtlcomputepipelinereflection/bindings) for direct operands and explicit `useResource` declarations for indirect operands. Reflection is requested at pipeline compilation; the prepared pipeline access mask selects the directly bound read/write resources during recording. The transport binds the existing canonical GPU buffer instead of making another alias. Its receive command declares completion of the external write to that buffer; its publication command declares the indirect SEND-cell write. A barrier is emitted only when the next command conflicts with accesses since the preceding barrier (RAW, WAR or WAW), following Apple's [ICB barrier contract](https://developer.apple.com/documentation/metal/mtlindirectcomputecommand/setbarrier()). The temporary access sets are destroyed before submission; no resource lookup, dependency traversal or hazard check runs during execution. The released native delegate creates its own device; the recording library uses Apple's [dyld interposition mechanism](https://github.com/apple-oss-distributions/dyld/blob/main/include/mach-o/dyld-interposing.h) to request indirect-command-compatible pipelines at creation. The reference executable is not linked to this library.

The reference provider is the same `liblitert-lm.dylib` used by the benchmark. [Dawn host-mapped buffers](https://dawn.googlesource.com/dawn/+/HEAD/docs/dawn/features/host_mapped_pointer.md) use the canonical pages; the native Metal view for transport names those same pages. Device and queue interception applies only during preparation. Dawn's first-use buffer initialization and native queue completion signals finish before recording returns. They are not replayed at decode boundaries. The prepared resource set owns the numerical bindings after the LiteRT executors are destroyed; the old native delegate's queue-residency bookkeeping is deleted.

Engine `tools/e2b_replay.m` constructs the whole native chain from `operands.json`, including both upstream embedding graphs, all 35 FFNs, attention and finishing consumers, and vocabulary projection. A partitioned manifest supplies distinct `layer_l/contribution_r` operand names; the same caller can execute all contributions locally. It destroys compiled LiteRT executors before GPU replay. The recorded kernels and their resources remain owned by `MetalReplay`. The requested decode interval is submitted once as one prepared indirect sequence. M13 commands are interleaved during preparation using native command copies, not a separate replay and barrier wrapper at each step; no host command construction, wait or operand staging occurs between steps. This remains replay of upstream kernels, not one across-step persistent numerical dispatch or a completed distributed implementation.

The manifest carries the upstream [packed-weight options](https://github.com/google-ai-edge/LiteRT-LM/blob/main/runtime/executor/llm_executor_settings_utils.cc) and [context replacement](https://github.com/google-ai-edge/LiteRT-LM/blob/main/runtime/executor/magic_number_configs_helper.cc). `--context` defaults to the reference's 2,048. The caller supplies `LiteRtMagicNumberConfigs` before model compilation, through writable private model mappings because replacement mutates shape metadata. Read-only `CreateModelFromFile` produced a protection fault in `ReplaceMagicNumberInSubgraph`; the private mapping changes metadata without rewriting model files. The causal boolean mask and runtime parameter operands follow the upstream executor utilities.

`output_data/e2b-metal-replay-20260918/replay-boolean-mask{.log,-run.json,-memory.json}` records the current complete local chain: 1,371 native commands, 3,325 resources, 22,003,712 canonical operand bytes, sixteen token-feedback steps, and clean client/bridge exits. Every step produced 262,144 finite, nonzero logits. Warm GPU execution spans 6.098–6.890 ms. This is a BOS-seeded GPU replay measurement with no prefill or RDMA, not the reference benchmark configuration or evidence of D2. Earlier runs used an incorrectly typed attention mask and are not model-performance evidence. The unchanged 180.54 tokens/s reference remains the denominator; the required distributed speedup is unestablished.

## Paired preparation and native limits

The M5 and Mini built engine `0f78994` and Mesh `ef23833` from committed Git objects. Both prepared 1,476 commands, 3,502 resources and 32,047,104 bytes of canonical operands for their assigned FFN shards. The attempted run stopped before GPU replay: M5 transport setup returned `EIO`; the Mini's connection attempt expired. Both clients returned 1 and both bridges returned 0. Peak observed wired memory was 8,066,318,336 bytes on the M5 and 2,195,554,304 bytes on the Mini, against planned per-node allocations of 9,967,504,336 and 9,589,832,656 bytes. Records are `output_data/e2b-rdma-replay-20260918/rank{0,1}-*.json` and logs in the engine checkout. No distributed timing is inferred.

The M5 provider reports `max_mr_size=16,384,000`, `max_qp=11`, and `PORT_DOWN`; the IP interface remains active and the Mini reports `PORT_ACTIVE`. The attempted source requested a 512 MiB registration and 70 QPs. M09 replaces the unsupported registration assumption with device-sized extents. M08 replaces one-QP-per-transfer with one bidirectional QP per configured independent stream and invocation slot: one QP for this pair's B=1 graph, with 36 receives preposted in the sender's prepared order. The dedicated TX cursor advances on acceptance and cycles across independent streams, never across the graph looking for a ready layer. These changes compile; the pair has not been relaunched while the port-down condition remains unexplained.

`output_data/e2b-metal-replay-20260918/replay-vocabulary-indexed{.log,-run.json,-memory.json}` records 16 completed GPU decode steps with both rank contributions executed locally, including the vocabulary pair and GPU token feedback. All 111 fragments compile; replay contains 1,600 commands, 4,033 resources and 31,653,888 canonical operand bytes. Client and bridge exit 0. Warm GPU times are 7.088–7.544 ms; this local execution of both shards is neither a paired measurement nor a replacement for the unchanged fast reference.

ABI 89 deletes the unobserved invocation/completion counters and status construction. Neither numerical execution nor the client reads them; they were extra atomic operations on every TX/RX completion. Page lifetime still follows client ownership and native queue teardown. The emitted native loops have no accepted-event register spills: TX uses an 80-byte frame, RX 64 bytes. The obsolete 37/37 and generated-kernel load table is replaced by the current native instructions in `hot-path-loads.md`.

`output_data/e2b-metal-replay-20260918/replay-prepared-boundary{.log,-run.json,-memory.json}` records 16 steps in one command buffer, 0.125310667 seconds of GPU execution, and clean client/bridge exits. The temporary boundary capture in `replay-boundary-values.log` observes the expected position, three cache parameters and mask word at all 16 boundaries; that diagnostic source was removed. Token sequences vary between runs; neither numerical equivalence nor improved throughput is established. Apple's [indirect dispatch contract](https://developer.apple.com/documentation/metal/mtlindirectcomputecommand/concurrentdispatchthreadgroups(_:threadsperthreadgroup:)) provides ordering before and after each range on the serial encoder. No additional boundary barrier is retained.

M14 follows Google's [prefill input construction](https://github.com/google-ai-edge/LiteRT-LM/blob/main/runtime/executor/llm_litert_compiled_model_executor.cc): the last prompt token remains pending for decode, and cache parameters are `[start, start+length, start+length, 0, 0, 0, 0]`. LiteRT's [input resize API](https://github.com/google-ai-edge/LiteRT/blob/v2.2.0/litert/c/litert_compiled_model.h) realizes the existing embedding functions for the prefill scope before buffer requirements are queried. The reference benchmark pads tokenized input to its requested length; the caller supplies those token IDs.

The original 1,024-token prefill and 256-step replay completed with clean exits (`replay-reference-256` in engine `output_data/e2b-metal-replay-20260918`): 0.044119375 s prefill GPU time and 1.673878292 s decode GPU time. This native Metal variant is below the unchanged 180.54 tokens/s WebGPU reference and does not establish D2. Preparation records identify two exported cache-index intermediates with incompatible physical requirements: 64-byte GPU producer storage and 16-byte host consumer storage. M15 removes that artificial graph boundary; shared addresses alone cannot reconcile those representations. The upstream residency union did not change the captured allocation count and is not evidence of the cause of the token variation.

M15 is constructed. `replay-prepared-indices` completes prefill and sixteen decode steps with clean client/bridge exits; the two intermediate index bindings are absent and canonical operand allocation falls from 53,149,696 to 52,953,088 bytes. The token stream still collapses to repeated IDs, so the binding defect was not a complete diagnosis. The measured fast implementation is the bundled WebGPU provider; M06 now records that provider's native Metal commands instead of substituting the native Metal delegate. Its Dawn host-mapped buffers must use the same M01 pages, with all capture and native bindings prepared before execution.

`replay-reference-provider-256` in engine `output_data/e2b-metal-replay-20260918` records the new path with 1,024 prefill tokens, 256 decode steps, 1,472 native commands, 3,864 resources, and 52,953,088 canonical operand bytes. Client and bridge both exit 0. GPU decode time is 1.967275333 s; prefill is 0.146950125 s. This still misses the unchanged fast reference and establishes no paired improvement. Repetition with the benchmark's zero-padded prompt alone is not evidence of an arithmetic defect. The body and embedding compilation options now follow the upstream distinction: [infinity capping for the language-model body](https://github.com/google-ai-edge/LiteRT-LM/blob/main/runtime/executor/llm_executor_settings_utils.cc), separate embedding options. The initial provider-capture exits were explicit unsupported-call exceptions for queue completion signals and one-time buffer initialization; their source causes were identified before relaunch. No GPU timeout or unexplained death occurred.

`replay-whole-cache-256` retains all 30 original KV outputs by their signature names and completes 256 steps in 1.964213333 s GPU time. With M06's reference device capabilities, `replay-reference-capabilities-256` completes in 1.605976375 s (159.40 tokens/s), with 1,350 commands, 2,814 resources, 43,188,224 canonical operand bytes and clean client/bridge exits. Planned allocations are 10,844,880,368 bytes; peak machine-wide wired memory is 12,675,809,280 bytes. Both records are in engine `output_data/e2b-metal-replay-20260918`; neither reaches the unchanged 180.54 tokens/s reference or measures RDMA. The earlier `replay-whole-reference-256` omitted KV outputs and is not performance evidence. Partitioned preparation now supplies 39 numerical calls per rank instead of 74, preserving 35 FFN publications and the vocabulary publication.

The superseded M06 vocabulary lowering is invalid: `replay-regions-capabilities-11-5.log` exposes the provider emitting `bitcast<i32>(vec2<f32>)` and its inverse, invalid under [WGSL's bitcast rules](https://www.w3.org/TR/WGSL/#bitcast-builtin); the client now exits on the native compilation error, and this run's timing is invalid. M16 deletes this representation path without changing projection precision. The reference ML Drift compiler core is unchanged.

| <a id="M16"></a>Native MAXLOC operand | `A + P*q(r,s,vocabulary)`; logits and index use their existing M01 buffers; all native bindings occupy M06 | 8 bytes, `MaxLoc` `_Static_assert` in `tools/e2b_replay.m`, MSL `static_assert` | Engine `tools/prepare_e2b.py:vocabulary` uses the caller's `--vocabulary-placement` (defaulting to `--placement`) to emit projection/ARG_MAX and native pair functions; `tools/e2b_replay.m:main` binds them before recording | Producer: index load, indexed FP16 logit load, two-word final pair store. Consumer: one pair load per prepared rank binding, scalar comparison, token store. FFN and vocabulary placement are independent setup operands; no runtime placement choice or candidate-array construction. |

M16 executes in `replay-canonical-maxloc-16` (engine `output_data/e2b-metal-replay-20260918`): 16 local steps with both rank contributions, 0.171023458 s GPU time, 2,804 commands, 6,311 resources and 53,739,520 canonical bytes; client and bridge exit 0. The preceding operand inspection found a stale native read through separately imported resource objects (`replay-maxloc-operands`); after M01 imports each operand once, both pair indices match the reference ARG_MAX outputs (`replay-canonical-resource`). This is a scoped binding check, not numerical equivalence. The temporary dump is removed. The whole unsplit graph completes 256 steps in 1.587863000 s, 161.22 tokens/s (`replay-canonical-solo-256`), still below the unchanged 180.54 reference. Planned allocations are 11,218,777,408 and 10,844,882,528 bytes respectively; observed machine-wide peak wired memory is 11,700,338,688 and 11,581,325,312 bytes. Neither run uses RDMA or establishes D2.

M06 reflected dependencies execute in engine `output_data/e2b-metal-replay-20260918/replay-reflected-dependencies-256`: 1,095 barriers for 1,350 decode commands, replacing 1,349 unconditional barriers; 256 steps take 1.414334250 s GPU time (181.003889 tokens/s), with clean client/bridge exits. The original 180.54 reference remains unchanged; no distributed speedup is inferred. The partitioned chain with both rank contributions also completes 16 local steps (`replay-reflected-partition-16`), with 2,241 barriers for 2,804 commands and 0.150579250 s GPU time. Planned bytes remain 10,844,882,528 and 11,218,777,408 respectively; observed machine-wide peak wired memory is 11,659,821,056 and 8,137,752,576. The earlier `replay-dependencies-16` omitted direct-buffer dependencies because this Dawn path does not issue indirect-resource declarations; its token IDs were invalid and its timing is discarded. Reflection supplies those direct accesses without changing numerical binding mode. Both machines build the committed source. M5 RDMA remains enabled but every native port reports down; the paired run and across-step persistent numerical dispatch remain unestablished.

The invocation measurement in engine `tools/e2b_replay.m:main` brackets submission through final completion with the host monotonic clock. M14 now shares one encoder and command buffer with decode; the native GPU interval and host interval both cover the complete invocation. Subtracting an overlapping command-buffer interval was not an established decode wall-time bound. M06 preparation can request Dawn's upstream [`dump_shaders`](https://github.com/google/dawn/blob/v20260720.160313/src/dawn/native/Toggles.cpp) through `DUMP_SHADERS`; it exposes the actual compiled representation without changing replay or adding per-event instrumentation.

`replay-dependencies-wall-256` records 256 steps with a 1.412772375 s native decode interval and a 1.568640667 s host invocation interval including prefill. Its former 1.430728500 s derived decode bound is withdrawn. Client and bridge exit 0; peak machine-wide wired memory is 11,184,095,232 bytes. `output_data/e2b-rdma-replay-20260919/paired-256` completes on both nodes, with host invocation intervals of 1.902293000 s and 1.913847000 s, respectively, and native decode intervals of 1.874385083 s and 1.885231500 s. Peak wired memory is 11,599,003,648 and 5,614,518,272 bytes. The 180.54 reference is unchanged; the required speedup is unestablished.

M07/M17's disjoint metadata views remove 36 false barriers from the 1,502-command decode program (1,220 to 1,184). `output_data/e2b-rdma-replay-20260919/disjoint-256` records 256 paired steps, matching rank token streams and clean client exits. M5/Mini host invocation intervals are 1.911184167/1.915126375 s including prefill; native decode intervals are 1.756689208/1.893469375 s. Planned bytes are 13,242,410,378/12,786,881,940; peak machine-wide wired bytes are 11,697,913,856/5,868,765,184. The M5 bridge retained its device context across the preceding intentional peer failure and this invocation; physical cable recovery remains unmeasured. No crossing-latency or D2 pass is claimed.

M14's single ordered encoder executes in `ordered-prefill-256` under that same output directory: 256 steps, host invocation intervals 2.031558375/2.031449084 s, matching rank tokens and clean exits. `placement-7-1-256` changes only caller placement and takes 1.861803208/1.861862542 s; `placement-7-1-vocab11-5-256` uses M16's independent vocabulary placement and takes 1.862038875/1.873839458 s. These include prefill and establish no decode speedup. The respective planned bytes for the latter two runs are 13,470,172,752/12,559,115,856 and 13,431,244,368/12,598,044,240; peak machine-wide wired bytes are 11,691,458,560/5,504,942,080 and 11,660,181,504/5,519,212,544.

M06 compiler output in `shader-full` and `shader-rank0-log` contains 136/139 distinct WGSL kernels. The partial down projections retain the packed 2-bit/4-bit weight reads and reduction structure; their post-sum output quantization is outside the partial. This is not a fallback to unpacked floating weights. The full preparation's original byte plan omitted repeated prefill model uses; `shader-full/planned-bytes-reconciled.json` records the corrected 13,621,541,440-byte plan and observed peak. Subsequent run preparation sums the actual selected manifest stages before launch.

`output_data/e2b-rdma-replay-20260919/metal4-local-256` and `metal4-7-1-256` record engine `2e6da94` executing through M19. Apple’s native heap frequency is 24 MHz on both devices; the preceding `metal4-local-16` measurement used the wrong conversion and its reported decode interval is invalid. The corrected M20 local interval is 1.394982458 s for 256 tokens. Paired M5/Mini intervals are 1.685542208/1.478454875 s; the M5 interval includes waiting for the longer Mini prefill. Whole paired host invocations are 1.855643916/1.855701625 s. Both clients exit 0 and both native contexts remain usable without reconnecting the cable. Planned bytes are 13,470,172,752/12,559,115,856; peak machine-wide wired bytes are 11,590,139,904/5,522,161,664. With engine `f4039f2` and mesh `ac93fd4`, `optimized-local-256` records 1.387196625 s and `optimized-7-1-256` records 1.677327750/1.474878792 s for decode and 1.846394084/1.846490417 s for the whole host invocation. Both clients exit 0 with matching 256-token streams after ordinary bridge teardown/restart; peak wired bytes are 11,875,729,408/5,527,207,936. Native range optimization does not close the D2 gap. Physical cable-loss recovery and the crossing floor remain unmeasured.

Engine `1484b27` prepares the original rotary expressions through the upstream operation for both signatures. `output_data/e2b-rdma-replay-20260919/upstream-rope-local-256` records 1,095 commands/920 barriers and 1.342851042 s decode for 256 steps, versus 1,350/1,095 and 1.387196625 s before lowering. `upstream-rope-7-1-256` records 1.620762875/1.422621250 s decode and 1.801779208/1.815451209 s whole host invocation on M5/Mini; the M5 decode interval includes waiting for Mini prefill. Both clients exit 0 with matching 256-token streams. Planned bytes are 13,470,720,912/12,559,664,016; peak wired bytes are 11,690,967,040/5,520,293,888. The pair remains below the D2 objective; no cable reconnect was needed and no physical replug was tested.

Engine `702db3b` also lowers the ten partial rotary expressions: decode records 1,028 commands and 875 barriers. Engine `70c8349` moves M20’s first timestamp past the first completed step without adding a timestamp or synchronization. `output_data/e2b-rdma-replay-20260919/steady-placement-results.json` records 255 measured steps out of 256 requested: modified solo 1.326020708 s (192.304689 tokens/s); 7:1 pair 1.397105500/1.397117958 s (182.518590 tokens/s using the slower rank); 2:1 pair 1.493020625/1.493036583 s (170.792868 tokens/s); 5:1 pair 1.418378917/1.418393250 s (179.780889 tokens/s). Every client exits 0, and each pair’s 256-token streams match. Across these runs planned bytes stay at or below 13,622,195,808 on M5 and 12,812,863,984 on Mini; peak wired bytes stay at or below 12,041,879,552/5,644,058,624. The unchanged baseline is 180.54 tokens/s and the D2 target is 198.594; none of the paired runs meets it or exceeds the modified solo result.

Engine `0f83cf3` replaces the standalone receive dispatch with a completion operand compiled into each original numerical reader. The program has 1,143 commands per decode step, 36 fewer than before. Mesh `e78c718` preconstructs distinct native receive records and completion words for the requested steps; `9dc6f8b` places the RX action at one aligned 32-byte record line. No completion word is reset by a consumer. Engine `443f58b` retains only pipelines actually used by the recorded program. These changes remain native kernel replay, not an across-step persistent numerical dispatch.

Engine `output_data/e2b-rdma-replay-20260919/direct-consumer-results.json` records 255 measured steps from each 256-step invocation: direct consumer 184.205349 tokens/s; independent vocabulary placement 184.274801; caller per-layer placement 185.158572; aligned vector publication 186.100579; subsequent recovery run 185.547195. Every completed pair exits 0 with matching token streams. These single-run differences are observations, not a variance estimate or a D2 pass. The unchanged reference is 180.54 and the target remains 198.594. Maximum planned bytes across these runs are 13,470,848,992/12,598,720,480 on M5/Mini; observed peak wired bytes are 12,027,920,384/6,041,665,536. Sparse diagnostic samples in `direct-consumer-trace-64` include the first numerical consumer dispatch and an approximately 12 ms outlier; they do not establish the crossing floor.

`direct-consumer-peer-loss/injection.json` records deliberate SIGTERM to the experiment bridge after pairing and during the submitted invocation. Both clients retire with `ECANCELED` (89), the bridge exits 0, and the remote controller records connection reset. `direct-recovery-256` then completes after ordinary bridge restart: decode intervals 1.374301333/1.374313417 s, planned bytes 13,446,764,512/12,583,876,576, peak wired bytes 11,989,712,896/5,808,013,312. No cable intervention or forced signal was used. This exercises the M12 cancellation binding inside the numerical readers and normal native teardown/reinitialization; physical cable-loss recovery remains unmeasured.

M24 builds on both nodes with `-Wall -Wextra -Werror`. Engine `af5f299` and Mesh `e6b21d1` execute `output_data/e2b-rdma-replay-20260919/native-link-events-256/result.json`: both clients exit 0 with matching 256-token streams; 255 measured steps take 1.371739583/1.371751625 s, or 185.893711 tokens/s on the slower rank, below 198.594. Planned bytes are 13,446,764,512/12,583,876,576; observed peak wired bytes are 12,000,280,576/5,816,516,608. The existing bridges first retired normally with SIGTERM; the new native notification subscriptions coexist with successful transport and ordinary invocation teardown. No interface was reconfigured or cable disconnected. M24's physical link-off branch remains unexercised; this result establishes neither a cable-loss recovery guarantee nor H7 latency.

M06 compiler reductions execute on both ranks in `output_data/e2b-rdma-replay-20260919/{native-scratch-256,simd-reduction-256}/result.json`: both clients exit 0 with matching 256-token streams. Removing redundant scratch initialization measures 187.395428 tokens/s; additionally lowering projection reductions measures 189.565685 tokens/s (255 steps, 1.345166708/1.345180167 s). Both remain below 198.594. Planned bytes are 13,446,764,512/12,583,876,576; the latter run peaks at 12,135,235,584/6,061,146,112 wired bytes. These individual observations do not establish repeatability or H7.

Engine `1f8d680` executes normalization register lowering in `output_data/e2b-rdma-replay-20260919/simd-normalization-256/result.json`: both clients exit 0, 256 tokens match, and 255 measured steps take 1.322199875/1.322213125 s (192.858470 tokens/s). The caller-only vocabulary placement `4,1` measures 191.764606 in `simd-normalization-vocab4-1-256/result.json`, so `rope-layer-7-5` retains its `7,1` vocabulary split. Engine `b502be3` additionally compiles exact two-bit extraction in `packed-extraction-256/result.json`: both clients exit 0, tokens match, and decode measures 193.140660 tokens/s. That 0.15% difference is one observation, not an established isolated improvement. Planned bytes remain 13,446,764,512/12,583,876,576 on the selected placement; peak wired bytes in the latter run are 12,000,346,112/5,820,121,088. None reaches 198.594 or establishes H7.

Engine `e156cf5` replaces the remaining normalization tree with upstream two-level SIMD sums. `output_data/e2b-rdma-replay-20260919/two-level-256/result.json` measures 194.598660 tokens/s. Three subsequent identical runs in `two-level-repeated-result.json` measure 195.696387, 196.022561 and 190.342873; pooled throughput is 193.985328, median 195.696387. Each rank exits 0 with a matching 256-token stream. Every run remains below 198.594; no repeatability or crossing-floor pass is inferred from the best observation. Planned bytes remain 13,446,764,512/12,583,876,576; peak wired bytes across the repeated runs are 13,028,884,480/5,810,290,688. Native TX/RX remain seven accepted-path loads at depths 0/1. Physical cable-loss notification is still unexercised.

M08 configures the SEND-posting and shared-CQ workers at creation using Apple's [pthread QoS attributes](https://developer.apple.com/library/archive/documentation/Performance/Conceptual/power_efficiency_guidelines_osx/PrioritizeWorkAtTheTaskLevel.html). This replaces inherited default priority: the active-thread snapshot records priority 31 for both. The attributes are destroyed immediately after creation. No affinity claim, runtime policy change or hot-path load is introduced. Apple's scheduler retains core placement; the change does not itself establish a latency bound.

Engine `d44ee24` specializes fixed integer dimensions before recording. `output_data/e2b-rdma-replay-20260919/scalar-specialization-matched-result.json` records three clean paired runs at 145.803913, 204.212762 and 208.579246 tokens/s, versus three same-compiler solo runs at 219.185909, 219.073937 and 218.757904. Pooled pair/solo are 181.271939/219.005767: S is 1.004054 against the unchanged 180.54 reference, but 0.827704 against the matched compiler. The earlier 209.441901 paired observation is not a repeatable RDMA advantage. The 11:5 placement measures 188.769801 and is not selected. All clients exit normally and paired tokens match; planned bytes remain at most 13,622,195,808/12,787,556,320. The slow paired repeat adds about 0.526 s; bridge logs contain no transport failure. Scheduling remains a hypothesis, not an established cause. No D2 or H7 pass is claimed.

The explicit attention-owner placement in engine `866db26` executes and retires normally but is removed: `attention-owner-256/result.json` measures 188.877323 tokens/s at 11:5; `attention-owner-3-1-256/result.json` measures 199.988510 at 3:1. Its extra input crossing loses to duplicated attention, which measures 207.140751 with Mesh `5eeeaf0` in `progress-qos-256/result.json`. These are single observations, not repeatability or H7 evidence. Each pair emits matching tokens. The owner trials plan at most 13,319,087,456/6,291,176,624 bytes and peak at 11,963,826,176/2,346,647,552 wired bytes; the QoS trial plans 13,446,764,512/12,583,876,576 and peaks at 12,020,514,816/5,810,094,080. None establishes an advantage over the matched 219.005767 solo result. The source retains explicit operand destinations and removes attention ownership, its rank filtering and optional boundary state.

Engine `92fee79` and Mesh `5eeeaf0` execute the retained explicit routes in `output_data/e2b-rdma-replay-20260919/explicit-routes-result.json`: both 256-step runs retire normally with matching rank tokens and measure 208.054270 and 208.525404 tokens/s. Planned bytes are 13,446,764,512/12,583,876,576; peak wired bytes across the two runs are 12,536,610,816/5,788,712,960. These exceed the original 180.54 reference but remain below the same-compiler 219.005767 solo result; they establish no RDMA speedup or H7 bound. Native accepted paths remain seven loads at depths 0/1. Numerical execution remains prepared ICB replay, not an across-step persistent numerical function. No physical cable disconnect occurred.

M06 publication lowering follows the MLX authors' [system-coherent access and system-fence mechanism](https://github.com/ml-explore/mlx/blob/main/mlx/backend/metal/kernels/fence.metal). `mesh_metal_publication_prepare` constructs an asserted 24-byte host binding (`records`, `completion`, `count`) for each canonical output before recording. `MetalRecorder.publish` registers the output, prepared M10 records, publication pipeline and notice resource; M06 inserts publication immediately after the native dispatch that writes that output. The caller no longer appends publication at a model-fragment return. The temporary output-to-publication mapping exists only during preparation, and all numerical bindings, SEND stores and barriers remain native commands before submission. `MetalRecorder.publish` marks canonical output buffers during preparation; the compiler lowers stores in their writing pipelines before recording, using the upstream emitter's explicit device-pointer stores. A forced-inline typed store preserves the original value and address, performs the coherent write, then the system fence, including paths that return immediately afterwards. M10's prepared dependency barrier orders completion of all producer workgroups before the SEND-cell store. This replaces MLX's separate payload visibility pass rather than omitting visibility. The publication set and source matching are preparation-only; no output lookup or type derivation executes on the crossing. Metal's public [memory-coherency specification](https://developer.apple.com/metal/Metal-Shading-Language-Specification.pdf), section 4.8, covers device scope; system scope uses the same compiler extension as MLX and is not claimed as a public Metal guarantee.

M07 observation is performed by the first thread of each numerical workgroup, matching MLX's single-thread [fence observer](https://github.com/ml-explore/mlx/blob/main/mlx/backend/metal/kernels/fence.metal) within the existing numerical dispatch. A threadgroup barrier distributes visibility to its numerical threads; only the existing M12 cancellation outcome is shared locally. This replaces every thread independently polling and fencing the same completion word, without adding a dispatch, host action, SHM lookup or transport condition.

| Object | Address | Bytes | Constructed at start() by | Read by event | Loads to reach it |
| --- | --- | --- | --- | --- | --- |
| <a id="M25"></a>Numerical workgroup cancellation result | Native threadgroup storage base + compiler-fixed offset, one per dispatched workgroup; no global address or invocation lookup | 4 bytes; generated MSL `static_assert(sizeof(uint)==4)` | Engine `MetalComputeRecorder.recordDispatch` compiles static threadgroup storage and its observer into M06; the prepared native grid supplies workgroups | Consumer entry reads local scratch after its observer; 0 SHM loads to address it. Only a missing-input cancellation sets this result; it does not gate publication or a native queue. | 0 |
| <a id="M26"></a>Bridge lifecycle notification | `H + offsetof(struct hdr, control)`; replaces the existing retirement notification integer in place | 8 bytes, aligned to 8; field extent/alignment asserted in `mesh.h:66` | `mesh-flow.c:465` binds the shared header before exposing the mapping; `mesh-call.c:100`, `mesh-dataflow.c:53` and `mesh-flow.c:60` notify Apple's shared-address waiter | No crossing event reads it: 0 T1/T2 loads. The bridge main thread observes setup/retirement with [os_sync_wait_on_address](https://developer.apple.com/documentation/os/os_sync_wait_on_address) instead of spinning while idle or while the independent progress threads run. [Shared-address wake](https://developer.apple.com/documentation/os/os_sync_wait_on_address_flags_t/os_sync_wait_on_address_shared) matches the physical shared word across processes. Notification follows the control-state store, and compare-and-wait closes the missed-wake interval. No timer, additional thread, per-step notification, publication wake or CQ wake. | 0 |

M26 removes one runnable lifecycle spinner per bridge: active population is main + one controller per link + two native progress threads per active link; only SEND posting and native CQ draining spin. Idle bridges consume no polling core. On the existing pair, passive `ps` observes each of the three idle bridges per node consuming approximately 100% of one CPU core; that is not useful RDMA progress. Native cable/error handling and explicit shutdown keep their existing teardown owner. Shared-memory ABI changes from 91 to 92 because old clients do not issue the lifecycle notification.

Mesh `b5abf2f` and engine `2670dad` execute M26 in `output_data/e2b-rdma-replay-20260919/lifecycle-{16,256,recovery-256}`: all clients exit 0, paired token streams match, and both updated experiment bridges return to sleeping state at 0.0% observed CPU. The 256-step runs measure 215.510350 and 215.291562 tokens/s, still below matched solo 224.886589; no isolated throughput gain or H7 pass is claimed. Planned bytes remain 13,446,764,512/12,583,876,576; peak observed wired bytes across those two runs are 12,266,045,440/5,848,907,776. `lifecycle-cancel/injection.json` records deliberate SIGTERM during the invocation: both GPU clients retire with ECANCELED (89), the active bridge exits 0, and the other bridge subsequently exits 0 when signalled while idle. Ordinary restart then completes `lifecycle-recovery-256`. No forced signal, interface reconfiguration or physical cable disconnect occurs. The emitted accepted TX/RX paths retain seven loads at depths 0/1; M26 adds none.

Engine `460be98` with Mesh `e0c2491` executes coherent producer stores in `output_data/e2b-rdma-replay-20260919/coherent-producer-256/result.json`: 209.710228 tokens/s. Engine `0c8e99d` executes workgroup observation in `workgroup-observer-256/result.json`: 210.314598 tokens/s. Both pairs retire normally with matching 256-token streams. `workgroup-observer-cancel` deliberately terminates the experiment bridge with SIGTERM; both clients retire with ECANCELED (89), and the bridge exits 0. Ordinary restart then completes the 256-step run. Physical cable loss remains untested.

Engine `8423cc3` specializes immutable scalar coefficient bits in `coefficient-matched-result.json`: pair 215.861229 tokens/s (255 steps in 1.181314500 s on the slower rank), matched solo 224.886589 (1.133904875 s), S = 0.959867. The pair remains slower than the matched solo; the original 180.54 reference does not establish an RDMA gain from compiler changes shared by both. Paired planned bytes remain 13,446,764,512/12,583,876,576, with observed peak wired bytes 11,769,479,168/5,832,884,224. These are individual observations, not a repeatability or H7 pass. `coefficient-crossings-64` retires normally; its sparse samples include numerical-consumer work and instrumentation, so they remain upper bounds. Native FFN producer grids are 96 or 192 workgroups, not the previously suspected six. No dispatch geometry change is justified by that hypothesis.

| Object | Address | Bytes | Constructed at start() by | Read by event | Loads to reach it |
| --- | --- | --- | --- | --- | --- |
| <a id="M27"></a>Native crossing trace, diagnostic build only | `trace_base[direction] + 32*accepted_event`; bases and capacity fixed by prepared SEND/RECV counts times invocation count | 32 bytes per event, aligned to 32, `_Static_assert(sizeof(struct mesh_trace)==32)` | `mesh-flow.c:link_prepare` allocates each direction before progress starts; `link_close` prints only after joining progress threads | Diagnostic TX records cell identity and observation/pre-post/post-return times. Diagnostic RX records native receive identity and poll-return/completion-store/repost-return times. The trace cursor is held by its progress thread; no trace code or allocation exists without `MESH_TRACE` at compilation | 0 |

M27 uses Apple [clock_gettime_nsec_np](https://github.com/apple-oss-distributions/Libc/blob/main/gen/clock_gettime.c) with `CLOCK_UPTIME_RAW`. Each trace is local to its node. Native post-call intervals include provider work; they do not identify the hardware doorbell instruction. Timestamp reads perturb the recorded native intervals; no cross-node clock alignment, CPU/GPU epoch equivalence, H7 pass or uninstrumented latency is inferred. The fixed receive record identity gives the prepared transfer and invocation by arithmetic after execution; SEND identity and accepted order give the corresponding route and occurrence. The diagnostic setup record supplies `receive_stride=C` for invocation-major M08 storage and `trace_binding` records associate each cell/frame index with its existing transfer identity, slot and chunk at configuration; offline decoding uses `step, frame = divmod(record_index, C)`. Placement fitting now requires the recorded transfer bindings; historical traces without them are not assigned an inferred all-to-all mapping. This descriptor is printed only during preparation, never read by a transport event. M22 and M27 are absent from ordinary submission and transport respectively.

Engine `3e66a9f` and diagnostic Mesh `588c325` execute `output_data/e2b-rdma-replay-20260919/crossing-calibrated-64`: 64 steps retire cleanly on both nodes. `crossing-timing.{json,csv}` joins all 36 sampled GPU crossings to their native records; each native direction records 2,304 events. M5/Mini native SEND-call medians are 84/83 ns (p95 125/84 ns); poll-return-to-completion-store medians are 125/166 ns (p95 291/209 ns). These include clock-read perturbation and approximately 42 ns quantization; zero-sized recorded intervals are below timer resolution, not zero work. GPU publication-dispatch medians are 5.792/5.625 us. The sampled layer-5 M5 numerical region lasts 664.375 us against the Mini’s 114.417 us, while the Mini’s following consumer interval grows to 568.750 us; corresponding native SEND calls are 208/42 ns. At vocabulary, numerical regions are 276.333/183.083 us and the Mini’s consumer interval is 107.958 us. These locate producer imbalance and GPU-region stalls separately from native posting; no one-way wire time, CPU/GPU epoch alignment or H7 pass is established. Adjacent no-work GPU timestamps resolve to the same tick and do not calibrate sampling-induced serialization. The M5 registry snapshot during client preparation reports 51% device utilization before model dispatch, so other GPU activity is present; its causal contribution to any individual stall is unestablished. Planned bytes including native tracing are 13,446,912,032/12,584,024,096. The single-SIMD receive variants recorded 199.139302 and 196.828330 tokens/s under the observed machine conditions and established no improvement; their source is deleted in `3e66a9f`. The retained numerical compiler and consumer entry match `8d13d39`.

M06 publication fusion follows Apple’s [native grid and threadgroup contract](https://developer.apple.com/documentation/metal/calculating-threadgroup-and-grid-sizes). Pipeline reflection identifies used buffer and dynamic threadgroup-memory slots during preparation. A dispatch inherits encoder state only for the slots its function uses; an earlier function’s scratch binding is omitted from the recorded command. A remote-consuming function already contained in one workgroup keeps its native geometry. Otherwise, a global-index elementwise function with no scratch, local indices, barriers or SIMD-group operations is regrouped into one workgroup when its complete grid fits the device limit. Original coordinates are reconstructed from preparation-time dimensions. M10 literal SEND stores precede the existing M07 observer in that same workgroup, after the native producer dependency barrier. The producer’s payload is complete before publication, and no other workgroup must become resident to publish. The recorder deletes the standalone publication command and carries its dependencies and residency into the consuming command. No independent-work fusion path remains.

Engine `22becf7` realizes publication fusion at all 36 crossings in `output_data/e2b-rdma-replay-20260919/publication-grid-64`, with decode command count 1,143 → 1,107 and matching rank token streams. Instrumented combined publication/consumer medians are 11.2705/14.1875 µs; maxima 342.167/335.917 µs remain. The uninstrumented `publication-grid-256` pair completes 255 measured steps in 1.252566042 s (203.582080 tokens/s); adjacent unchanged-numerics solo `publication-grid-solo-256` takes 1.199209250 s (212.640121 tokens/s). No throughput improvement or H7 pass is established. Planned pair bytes are 13,446,764,512/12,583,876,576 and observed wired peaks 11,868,880,896/5,854,887,936 bytes. Both clients retire with exit 0.

Engine `cd618bf`, mesh `8ebee7d`, `output_data/e2b-rdma-replay-20260919/crossing-clock-64/crossing-timing.{json,csv}` correlate all 36 GPU samples with 2,304 native events in each direction per rank. Before/after native CPU and GPU samples are equal in nanoseconds on both devices; Metal 4 ticks scaled by 24 MHz fall within those samples, giving an empirical local common-epoch mapping. Maximum raw-clock brackets are 2,625/1,500 ns, too wide for an H7 sub-microsecond pass. At layer 1/step 2, the M5 numerical interval is 703.750 µs, followed by 160.583 µs until TX observes publication; the receive completion already precedes outgoing post-return by 744.042 µs. The Mini posts in 83 ns, then spends 758.917 µs before its local completion publication and 2.083 µs to the consumer-end sample. At layer 26/step 27, boundary-to-TX-observation is 373.208/316.500 µs while native post calls are 83/83 ns. These locate delayed local publication and remote producer availability, not wire time. GPU timestamp commands [finish prior work without delaying subsequent work](https://developer.apple.com/documentation/metal/mtl4computecommandencoder); an outgoing host SEND may therefore follow the local consumer, which only requires its incoming operand. Signed event differences are retained. No CPU/GPU interval is promoted to a sub-microsecond claim, and clocks between nodes are not aligned. Both clients and diagnostic bridges retire with exit 0; ordinary non-tracing bridges are restored.

The offline placement caller uses SciPy [milp](https://docs.scipy.org/doc/scipy/reference/generated/scipy.optimize.milp.html), backed by Huangfu, Galabova, Feldmeier and Hall’s HiGHS. M27 prints each prepared SEND cell and RECV frame’s existing transfer binding during configuration; diagnostic events retain their fixed record identity. The manifest maps that binding to its actual producer, consumer set and layer. Offline decoding never infers an all-to-all route from a layer index or expects a zero-width rank to publish. Each measured transition is keyed by both the preceding and current complete column vectors, retaining changes to the graph when a singleton publishes the finished activation. Binary variables select observed adjacent transitions, with continuity constraints between layers. Finish-time inequalities contain only the selected transition’s actual contributing predecessors and directional handoff costs; zero-width ranks have no publication node. Bounds are finite prefix sums of the largest recorded nonnegative work and handoff intervals and relax unselected edges only. All measured samples remain in the report; the objective is final publication completion per sampled invocation. This expands the existing [timed-dataflow recurrence](https://ptolemy.berkeley.edu/projects/embedded/eecsx44/lectures/Fall2014/wk7-lec2-dataflow-throughput.pdf) over measured topology choices, rather than combining widths measured under different predecessor graphs or inventing missing publications. The solver emits column shares only; no finish time, profile, variable, timing operation or decision enters execution. Node clocks are never subtracted across machines. Predictions are counterfactual placement inputs, not measured throughput or a crossing-latency pass; the final consumer tail remains outside native publication timestamps.


M06 construction in engine `4ca3a03` records all 35 FFN SEND stores inside their original numerical consumer entries and the vocabulary SEND store inside its producer. `output_data/e2b-rdma-replay-20260919/whole-reflected-memory-capture/prepared.json` contains zero standalone publications and 1,107 decode commands, down from 1,142. Pipeline reflection removes unused inherited threadgroup allocations; the broader independent-work fusion cases are deleted. The ordinary `whole-reflected-memory-256/result.json` pair retires cleanly in 1.202660167 seconds for 255 measured steps, 212.029971 tokens/s. Adjacent matched solo takes 1.195895917 seconds, 213.229259 tokens/s: S = 0.994376, below 1.10. Matching rank token streams and dispatch deletion do not establish speedup or the crossing floor.

M27 alone records `whole-native-trace-64/movement-timing.{json,csv}`: 2,304 sends and receives per rank, both clients and diagnostic bridges retired cleanly. Numerical kernels contain no timing samples. After excluding warmup, the positive local interval from SEND return to incoming CQ observation sums to a mean 253.893/293.230 microseconds per step on M5/Mini; these rank sums overlap and are not added together. M5 layer 0 has a 28.625 microsecond median signed interval, versus 16.042/15.625/14.417 at layers 4/9/14. Those intervals include remote producer availability and transport, not isolated wire time. Per-layer median native SEND calls are about 42–84 ns; CQ-to-input-store medians include timestamp perturbation and do not establish H7. This identifies producer availability at those cuts as the next placement input; it adds no event-time decision or numerical instrumentation to ordinary execution.
