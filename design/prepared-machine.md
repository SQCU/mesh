# Prepared machine

| Start-time integer | Value |
| --- | --- |
| `r, p` | Rank in `{0,1}`; `p` is the dense local index of the configured peer link. Rank 0 is M5 Max, rank 1 is M4 Pro. |
| `s` | Invocation slot in `[0,F)`; `F` is caller-supplied concurrent invocations, independent of layer count. Each independently published partial in each slot has its own native QP; one TX spinner serves the link. |
| `l` | Physical partial in `[0,L_r)`; `0..34` are FFN outputs, `35..L_r-1` tile the single vocabulary phase. |
| `H, V, B` | `1536`, `262144`, `1`. |
| `v_r` | Number of vocabulary columns assigned to rank `r` by the caller; vocabulary consumer is caller-selected rank 0. Layer partials still go directly to every rank. |
| `L_r, L` | `35 + ceil(v_r/60416)`; `L = max(L_0,L_1)`. |
| `b(r,l)` | `3072` for `l < 35`; otherwise `2*min(60416,v_r-60416*(l-35))`, at most `120832` bytes (118 KiB). |
| `S(r,l)` | `Q*ceil(b(r,l)/Q)`, reserved contiguous bytes per invocation of that operand. |
| `Q` | Native payload extent selected during connection setup, fixed for this prepared execution. |
| `c(r,l)` | `ceil(b(r,l)/Q)`, actual native requests for that push. |
| `k` | Native request in `[0,c(r,l))`; private to transport. |
| `j_rx(r,s,l,p,k)` | `first_rx(r,l,p)+s*stride_rx(r,l,p)+k`; canonical receiver row. Both section integers are fixed at binding, independent of SEND arena indices. |
| `P_r(l)` | Setup ordinal among independently published partials; equal source rows are adjacent for publication fan-out. Excludes the once-only prefix. |
| `j(s,l,p)` | `o(r)+s*n(r)+P_r(l)`, directly naming one M04 record. |
| `n(r), o(r), N(r)` | Independent publications per slot, once-only publications, and `F*n(r)+o(r)` total; fixed by `mesh_transfers_prepare`. |
| `h(j), C_rp` | Native request prefix for publication j, and sum of native chunks over all publications; both fixed at setup. |
| `A_rl, U_rlp, O_rlu, T_rp, X_rp, Y_rp, V_rp, Z_r, P_rsl, N_rp, CQ_output_r` | Start-bound producer, receive, numerical output, TX cells, native SEND requests, RECV records, RECV SGEs, canonical page table, GPU publication records, inline publication programs, CQ output bases. All CPU/GPU/NIC aliases are fixed at setup. |
| `CPU_r(offset), GPU_r(offset), NIC_r(offset)` | The three start-bound aliases of the same registered pages; no runtime translation between aliases. |
| `source_row(r,s,l)` | Canonical producer row integer, resolved by the caller’s tensor allocation at setup. |
| `R_r, S_control` | Canonical control base and slot stride `Q*ceil(C_control(r)/Q)`. `a_l=32*B*36+sum_{i<l}32*B*(1+K_i+G_i)`; `a_finish=32*B*36+sum_l32*B*(1+K_l+G_l)`; `a_project=a_finish+32*B*35`; `a_sample(r)=a_project+32*(L_r-35)`; `a_scope(r)=a_sample(r)+(r==0?32*B:0)`; `C_control(r)=a_stop(r)+32`, where U is the setup sum of batch rows in all vocabulary scopes on consumer rank 0, and zero on other ranks. `K_l,D_l,H_l,G_l=H_l/QPT_l,n_l` are fixed layer dimensions. |
| `g` | Invocation generation held in the participating native execution and transport registers before the first producer store. It is a stored value, never an address component. |
| `a, h, f, w` | Start-bound numerical notification ordinal, invocation-record ordinal, function identity, and worker index. `a` and `h` include the invocation slot. Normal decode has `F` input-preparation notification cells per rank; the resident step has no numerical-worker notification; optional trace calls add their declared cells. |
| `E_r, C_r, K_r, J_rf, G_r, End_rs, Sarg_rq` | Start-bound CPU-input notification arena, prepared call records, worker records, function metadata, call group, device completion cell base and supplied sampler-operand bases. |
| `S_call, S_recv` | `round_up(128+32*max_call_stores,128)` and `round_up(64+32*max_receive_stores,64)`, realized at setup; maxima are over actual declared fan-out. Worker registers retain the corresponding stride. |
| Constructor state | `37/37`; inline CPU/RX publication programs and direct contiguous CPU operands are constructed. M05, M12, M14, M16, M19 and M21 are retired. Resident numerical scope covers embedding, all 35 layers, final normalization, vocabulary projection, softcap, sampling and logprob capture. One prepared command remains live until explicit shutdown. No bank, successor, lease, replay or renewal timer exists. Vocabulary placement constructed at `metal-microbench/mesh_layer.swift:547` and selected by `bootstrap.swift:2000`: M34 publishes peer completion on its final local vocabulary tile; only rank 0 constructs M35/M36 sampling state/scratch and consumes logits. M37 peer completion has no sampler dependency. |

| ID / object | Address | Bytes and required `_Static_assert` | Constructed at `start()` by | Read by which event | Loads to reach it at runtime |
| --- | --- | --- | --- | --- | --- |
| <a id="M01"></a>M01 producer output | `A_rl+s*S(r,l)` | Reserved `S(r,l)`; active `b(r,l)`; scalar width `_Static_assert(sizeof(uint16_t)==2,"M01 M02 M03 scalar storage");` in `rdma/mesh-call.h:106`; extent is the start-bound section byte count. | `metal-microbench/mesh_layer.swift:539`; contiguous slot allocation/binding at `rdma/mesh-call.c:574` and `:567` | value-ready | 0 — native producer binding |
| <a id="M02"></a>M02 receive backing | `U_rlp+s*S(p,l)` | Reserved `S(p,l)`; active `b(p,l)`; same scalar assertion as M01; the registered extent is fixed at setup. | `rdma/mesh-call.c:520`, `rdma/mesh-call.c:527` | consumer read | 0 — native consumer binding |
| <a id="M03"></a>M03 numerical consumer output | `O_rlu+s*Q*ceil(bytes(r,l,u)/Q)`; `u` selects the caller-declared output section | Active `bytes(r,l,u)`, Q-rounded backing; finished hidden and PLE contribution each have `2*B*H` bytes; PLE input and gate each have `2*B*PLE` bytes; FFN sum stays in numerical threadgroup scratch; same scalar assertion as M01. Rank-0 per-slot sampling scratch: `8*B*ceil(V/1024)` partials, `4*B*ceil(V/1024)` argmax and masses each, `4*B*ceil(V/1024)*50` candidates. | `metal-microbench/mesh_layer.swift:539`, `rdma/mesh-call.c:574` and `:567`; numerical destinations bound at `metal-microbench/mesh_layer.swift:205` and `:284` | consumer read | 0 — native numerical destination |
| <a id="M04"></a>M04 direct native publication record | `T_rp+32*j(s,l,p)` | `32`, aligned 32: ready at +0, native QP +8, native request head +16, reserved +24. `_Static_assert(sizeof(struct mesh_send)==32 && _Alignof(struct mesh_send)==32,"M04");` | `rdma/mesh-call.c:494`–`:491`; native QP/request at `rdma/mesh-flow.c:138`–`:129`; GPU alias `swift/Mesh.swift:661`. | TX post | 0 — array base and scan ordinal held in registers. The ready word and both native operands occupy one line. No M16 cursor, no publication-cell pointer load, no cross-partial SEND ordering. Clear acceptance before native posting; no later clear can erase the next generation. |
| <a id="M06"></a>M06 native SEND work requests | `X_rp+128*(h(j)+k)`, `k<c(r,l)` | `128` each; native sizeof assertion in `rdma/mesh-flow.c:8`. | `rdma/mesh-flow.c:91`, linked native requests `:137`; native queues/depths `rdma/mesh-verbs.h:241`–`:258`. | TX post | 1 — head in M04. The provider walks its native WR chain; Mesh never reconstructs chunk arguments or waits between fragments. |
| <a id="M07"></a>M07 native SEND scatter/gather entry | `X_rp + 128*C_rp + 16*(h(j)+k)` | `16`; `_Static_assert(sizeof(struct ibv_sge)==16,"M07");` | `rdma/mesh-flow.c:146` | TX post | 1 — native request |
| <a id="M08"></a>M08 native RECV work request | `Y_rp+S_recv*j_rx` | Header `64`, aligned 64: WR +0, post/QP +32/+40, retirement row +48, inline publication count +52; instructions +64. Header/offset assertions in `mesh-flow.c`. | `rdma/mesh-flow.c:95` realizes S_recv; `:115` allocates records; `:170`–`:175` constructs headers and inline instructions. | RX completion | 1 — canonical native completion row. M10 and inline CPU/forward publications occur before the native repost; no receive-target array or loaded range base follows this header. |
| <a id="M09"></a>M09 native RECV scatter/gather entry | `V_rp+16*j_rx(r,s,l,p,k)` | `16`; `_Static_assert(sizeof(struct ibv_sge)==16,"M07 M09");` in `rdma/mesh-flow.c:9` | `rdma/mesh-flow.c:117`, `rdma/mesh-flow.c:167` | RX completion | 1 — completion row; native WR contains the same start-bound address |
| <a id="M10"></a>M10 receive availability record | `Z_r+32*j_rx(r,s,l,p,k)`; generation at `+24`; producer availability uses `Z_r+32*source_row(r,s,l)+24` | `32`; canonical `mesh_page_entry`, asserted in `rdma/mesh.h:39`, including `offsetof(stamp)==24`; arena base and stride are multiples of 32. | Backing at `rdma/mesh-call.c:527`; initial generation `s+1-F` at `rdma/mesh-flow.c:164`; native GPU alias at `swift/Mesh.swift:242` | consumer read | 0 — consumer holds the stamp address; RX addresses it by the completion integer |
| <a id="M11"></a>M11 completion output | `CQ_output_r + 48*p` | `48`; `_Static_assert(sizeof(struct ibv_wc)==48,"M11");` | `rdma/mesh-flow.c:432`; shared native CQ at `rdma/mesh-verbs.h:238` | RX completion | 0 — native poll destination held by transport |
| <a id="M13"></a>M13 inline publication instructions | CPU: `C_r+S_call*(f*F+s)+128+32*a`; RX: `Y_rp+S_recv*j_rx+64+32*a`; GPU: literal stores | `32`: destination +0, multiplier +8, padding +16. Swift-importable ABI alignment 16; runtime arrays start at 32/64/128-byte-aligned allocations with stride 32. Size/alignment and inline offsets are statically asserted. | `rdma/mesh-dataflow.c:262`; CPU `rdma/mesh-call.c:296`–`:324`, RX `rdma/mesh-flow.c:175`, GPU setup `swift/Mesh.swift:653`. | value-ready / RX completion | 0 — inline constant offset from the already held call/receive address, plus sequential instruction ordinal. Store `1+invocation*scale`; no publication pointer, operand walk, first/end indirection, value field or destination lookup. Resident GPU programs consume these bindings at setup. |
| <a id="M15"></a>M15 native post failure output | `SP_tx_rp+8` = `TFP_rp-56` | `8`; `_Static_assert(sizeof(struct ibv_send_wr *)==8,"M15");` | `rdma/mesh-flow.c:197`; emitted `_link_send_progress` reserves 80 bytes, FP=SP+64, failure output SP+8. | TX post | 0 — one native output location per TX thread, held directly; per-slot heap failure cells are deleted. |
| <a id="M17"></a>M17 thread handles / population | `link_base_r + sizeof(struct mesh_link)*p + offsetof(struct mesh_link,workers) + 8*d`, `d<4`; controller at that link's `controller` offset; polling lifecycle word at `offsetof(struct mesh_link,progressing)` | `8` per pthread handle; 4-byte lifecycle word; `_Static_assert(sizeof(pthread_t)==8,"M17");` | `rdma/mesh-flow.c:336`, `rdma/mesh-flow.c:457`; numerical workers at `rdma/mesh-call.c:383` | TX post | 0 — native thread entry argument. Chosen: one TX thread per link scans flat M04 records, one ready load per probe. Each directed partial/slot has a native QP, with the link’s SEND/RECV CQs shared. Queue count is the sum of prepared outgoing/incoming partials, with no additional thread per QP. Per arena with P links and W active numerical workers: `spinners <= 4P+W+1`, `threads <= 5P+W+1`; the extra 1 is the spinning bridge main, and P controllers block in kqueue. `W<=8`, independent of `MESH_QPS` and F; decode and prefill each configure W=1. The engine constructs both arenas: for the pair, P=1 per arena, total bound 12 spinners and 14 Mesh threads per rank, independent of F. Native driver, Metal and caller-owned threads are outside this Mesh-owned count. |
| <a id="M18"></a>M18 consumer notification cell | `E_rw+16*a`; call index at +0, invocation mask at +4, generation at +8 | `16`, aligned 16; `_Static_assert(sizeof(struct mesh_arrival)==16 && _Alignof(struct mesh_arrival)==16,"M18");` | `rdma/mesh-call.c:274`–`:305`, edge contents `:268`, contiguous consumer ranges `:288`. | input preparation | 0 — worker holds base and cursor. A 16-byte event carries the prepared M20 integer, the constant/varying mask and generation. Independent consumers have distinct cells; no use descriptor follows it. |
| <a id="M20"></a>M20 prepared call record | `C_r+S_call*(f*F+s)` | `128`, aligned 128; native submit/context +72/+80, publication count +88, recurrent dependency count +92, instructions +128. Header and offset static assertions in `mesh-call.h`. | `rdma/mesh-call.c:296`–`:324`, after consumer ranges are constructed. | input preparation / retirement | 1 — M18 call integer; base and S_call retained at worker entry. No object is rebuilt on invocation. Completion reads this same header and its inline fixed-offset M13 instructions. Numerical input counts remain declared dependencies. |
| <a id="M22"></a>M22 numerical worker state | `K_r+128*w` | `128`, aligned 128; `_Static_assert(sizeof(struct mesh_call_worker)==128 && _Alignof(struct mesh_call_worker)==128,"mesh_call_worker");` | `rdma/mesh-call.c:80`, `:295`, `:305`. | input preparation / retirement | 0 — entry argument; arrival and call-array bases load before polling. Cold retirement reader remains. |
| <a id="M23"></a>M23 function definition and lifetime metadata | `J_rf` | `112`; `_Static_assert(sizeof(struct mesh_function)==112,"M23");` | `rdma/mesh-call.c:99`, sections `:102`–`:107`; instance construction `:298`–`:327`. | input preparation / retirement | 1 — M20 function pointer, cold lifetime/reset only. Runtime submission does not read the definition. No rearm entry/context remains; native nonresident Metal constructs its single-use command at invocation. M20 carries the fixed recurring dependency count, restored by dispatch before invoking the supplied function, never by cold retirement. |
| <a id="M24"></a>M24 call-group lifecycle state | `G_r`; running at `+1204`, worker array at `+128` | `1280`; `_Static_assert(sizeof(struct mesh_calls)==1280,"M24");` | `rdma/mesh-call.c:75` | input preparation / retirement | 0 — captured by the numerical thread at entry; lifecycle polling is outside operand handoff instructions |
| <a id="M25"></a>M25 input/retirement worker and RX ABI frames | `FP_r-96..FP_r+15`; `RFP_rp-96..RFP_rp+15` | `112` each; scalar-size assertions in `rdma/mesh-call.c:35`, `rdma/mesh-flow.c:23`; actual extents from emitted -O2 assembly. | Native entry `rdma/mesh-call.c:134`, `rdma/mesh-flow.c:237`; frame publication `:145` and `:249`. | input preparation / RX completion | 0 — FP/SP. No RX hot-loop spill loads. Input dispatch has none; cold return handling reloads call-group base at FP-96. RX bad-WR output RFP-88. No native decode rearm frame exists. |
| <a id="M26"></a>M26 resident embedding continuation | `R_r+s*S_control+32*b`, `b<B`; `R_r` is the final canonical storage section allocated for decode | `32` per batch row: completed generation at +0, reserved +8, width +12, slot stride +16, scale +20. `_Static_assert(sizeof(struct prepared_residency)==32 && offsetof(struct prepared_residency,completed)==0,"M26");` in `rdma/mesh-call.h:70`; matching Metal assertion at `metal-microbench/kernels.swift:72`. | `metal-microbench/mesh_layer.swift:539`, initialized at `:23`; native bindings at `:35`, initial dispatch committed during start-time finalization at `:434` | consumer read | 0 — one SIMD group per `(s,b)` consumes token generations. Width, stride and scale load once at entry; generation remains in registers. No competing-bank embedding claim or completed-generation reload. M40 is read only for explicit shutdown. |
| <a id="M27"></a>M27 resident FFN continuation | `R_r+s*S_control+32*B*(l+1)+32*b`, `l<35`, `b<B` | `32`: gate next/done at +0/+4, down-next at +8, shared publication tile count in the first batch row at +12, input/completed generations at +16/+24. `_Static_assert(4*sizeof(uint32_t)+2*sizeof(uint64_t)==32 && 6*sizeof(uint32_t)+sizeof(uint64_t)==32,"M27 M28 M33 M34 M35");` in `rdma/mesh-call.h:76`; Metal `static_assert(sizeof(mesh_ffn_state)==32,"M27")` at `metal-microbench/kernels.swift:75`. | `metal-microbench/mesh_layer.swift:539`, zeroed at `:55`; resident source and native binding at `:387` and `:405`; attention directly stores its FFN input generation at `metal-microbench/kernels.swift:3159` | consumer read | 0 — native buffer base plus compiled constant offsets. One resident dispatch per slot contains all 35 layers; fixed weight, input, scratch, output and publication offsets are emitted at setup. Existing dot/quantization routines execute 32-column numerical tiles; device atomics claim and complete tiles, with no barrier over dispatched threadgroups. After all down tiles of a row are assigned, a workgroup continues immediately; it does not wait for their completion. Down completion increments the layer-wide count at the first row’s +12. The final tile across every batch row directly publishes M04/M10/M18 and the first row’s completed generation. This preserves full-operand publication when batch rows finish out of order. M40 requests explicit shutdown between finite numerical tiles; token generations remain numerical progress counters. |

| <a id="M28"></a>M28 resident attention continuation | `R_r+s*S_control+a_l+32*b`, `b<B` | `32`: project/split/output-next/output-done at +0/+4/+8/+12; input/completed generations +16/+24. `_Static_assert(4*sizeof(uint32_t)+2*sizeof(uint64_t)==32 && 6*sizeof(uint32_t)+sizeof(uint64_t)==32,"M27 M28 M33 M34 M35");` in `rdma/mesh-call.h:76`; Metal assertion at `metal-microbench/kernels.swift:78`. | `metal-microbench/mesh_layer.swift:492`, allocation `:534`, initialization `:100`, bindings `:117`; next-layer input producer `:221` | consumer read | 0 — native base plus compiled layer/row offsets. Attention and FFN stages share the existing resident dispatch, scratch and one explicit shutdown word. Projection claims numerical columns; split claims require the corresponding query group and its actual KV producer; output columns consume all reduced heads. Last output tile normalizes, adds the residual and stores M27.input directly. |
| <a id="M29"></a>M29 attention head continuation | `R_r+s*S_control+a_l+32*B+32*(b*(K_l+G_l)+h)`, `h<K_l+G_l`; KV heads first | `32`: column/split completion counts +0/+4, ready/reduced generations +8/+16. `_Static_assert(2*sizeof(uint32_t)+3*sizeof(uint64_t)==32,"M29");` in `rdma/mesh-call.h:82`; Metal assertion at `metal-microbench/kernels.swift:81`. | `metal-microbench/mesh_layer.swift:100`, `:117`; shared-KV producer selected at `:115` | consumer read | 0 — compiled array base, numerical head index. Last projection tile performs that head's norm/RoPE/KV write before ready publication. Last split reduces its query group and publishes reduced; no count of resident threadgroups or host event is used. Shared-KV layers bind the producer layer's head array at setup. |
| <a id="M30"></a>M30 attention split operands | `U_attention_rl+s*Q*ceil(E_l/Q)`; `E_l=4*B*H_l*n_l*(D_l+2)`; maxima +0, denominators `+4*B*H_l*n_l`, weighted values `+8*B*H_l*n_l` | `E_l`; `_Static_assert(sizeof(float)==4,"M30");` in `rdma/mesh-call.h:84`. | Section extent `metal-microbench/mesh_layer.swift:503`, canonical allocation `:534`, native addresses `:117` | consumer read | 0 — compiled buffer base and row offsets; head/split indexing is the existing numerical reduction. Distinct canonical backing per layer and invocation slot. |
| <a id="M31"></a>M31 attention index operands | Each start-bound `Index_rq`, where q names positions, write-skip, lengths, block table or full/partial CSR offsets/indices; row address `Index_rq+b*stride_q` | `4*elements_q` from each existing buffer's setup capacity; `_Static_assert(sizeof(uint32_t)==4 && sizeof(uint64_t)==8 && sizeof(uint16_t)==2,"M31 M32");` in `rdma/mesh-call.h:89`. | Existing operands retained at `metal-microbench/mesh_layer.swift:101`, fixed row bindings at `:117` | consumer read | 0 — compiled operand bases. Position, page and mask values remain numerical inputs to the existing RoPE, KV scatter and attention gather; they do not select collective bindings. |
| <a id="M32"></a>M32 KV chunk directory and backing | Directory `Karg_rl+8*c` / `Varg_rl+8*c`; chunk backing `K_rlc` / `V_rlc`; every c and alias bound at setup, shared-KV l resolves its producer at setup | Directory `8*chunkCount_l`; each chunk `2*chunkPages*16*K_l*D_l` active bytes; scalar/pointer assertion in `rdma/mesh-call.h:89`. | Actual cache resources retained at `metal-microbench/mesh_layer.swift:101`, native directory bindings at `:117` | consumer read | 0 to directory, 1 to chunk backing through the existing numerical KV operand. RoPE, KV scatter, paged attention and split reduction have one shared arithmetic implementation used by native and resident entries. |

| <a id="M33"></a>M33 resident layer-finish continuation | `R_r+s*S_control+a_finish+32*(l*B+b)`, `l<35`, `b<B` | `32`: input next/done +0/+4, gate next/done +8/+12, down next/done +16/+20, completed generation +24. The M27/M28/M33 extent assertion at `rdma/mesh-call.h:76` includes `6*sizeof(uint32_t)+sizeof(uint64_t)==32`; Metal `static_assert(sizeof(mesh_finish_state)==32,"M33")` at `metal-microbench/kernels.swift:84`. | Prefix `metal-microbench/mesh_layer.swift:500`, canonical allocation `:534`, initialization `:161`, compiled operand bindings `:205`; resident vocabulary binding `:261` | consumer read | 0 — native base plus compiled layer/row offsets. Partial stamps and payloads are literal bindings. Local and remote terms share one setup-unrolled poll. The first available term initializes numerical scratch; each remaining term is added exactly once. Register-local consumed flags cover all terms. No local-first availability gate remains in the reduction. Workgroups enter reduction once the FFN down work is assigned, while other groups may still be producing the local term. Full local visibility is an input of the same reduction, not an entry gate. PLE uses the original embedding and canonical token input; existing dot, activation and RMS arithmetic applies the layer projection, token embedding, gate, down projection, norms, residuals and scale. Input-done has one extra final-normalization count. Last down tile publishes the next M28.input and M33.completed; M40 explicit shutdown applies between claims and on every peer poll; a claimed tile finishes its stores and publication before exit. |

| <a id="M34"></a>M34 resident vocabulary continuation | `R_r+s*S_control+a_project+32*(l-35)`, `35<=l<L_r` | `32`: next/done +0/+4, completed +24; M34/M35 scalar assertion `rdma/mesh-call.h:76`, Metal `sizeof(mesh_output_state)==32` at `metal-microbench/kernels.swift:4551`. | Prefix/allocation `metal-microbench/mesh_layer.swift:528`, `:534`; zero/bind `:247`, `:262` | consumer read | 0 — compiled scope offset. Columns/rows and weight layout specialize the existing dot routine. Fixed M33 input stamp; final norm is numerical threadgroup work. Last tile publishes TX/local stores and completed generation. Nonconsumer ranks have no sampler or incoming logits; their last vocabulary tile stores its completed generation before publishing the final operand and M37 completion. Earlier vocabulary tiles are finished by the declared sequential scope computation. |
| <a id="M35"></a>M35 resident sampler continuation | Rank 0 only: `R_r+s*S_control+a_sample(r)+32*b` | `32`: partials-done +4, mass next/done +8/+12, final claim +16, completed +24; same assertions as M34 | `metal-microbench/mesh_layer.swift:529`, `:534`, `:247`, `:308` | consumer read | 0 — compiled row offset. Claims cover existing partial, mass, sample and logprob arithmetic. Completion advances generation across steps; the last batch row writes M37 after output visibility. |
| <a id="M36"></a>M36 sampler scope claim | Rank 0 only: `R_r+s*S_control+a_scope(r)+8*u`, `u<U` | `8`: next +0, reserved +4; `_Static_assert(2*sizeof(uint32_t)==8,"M36");` at `rdma/mesh-call.h:78`; Metal assertion at `metal-microbench/kernels.swift:4554` | `metal-microbench/mesh_layer.swift:530`, `:534`, `:247`, `:287` | consumer read | 0 — each peer/scope/row is bound at setup. Scope polling is unrolled; a ready scope claims a numerical 1024-element tile without waiting for another scope. Stop is checked before claiming; accepted finite work completes before exit. |
| <a id="M37"></a>M37 device-to-retirement event | `End_rs+8`, where each End_rs is the prepared one-cell stream base | `8` cell in a `128`-byte stream; `_Static_assert(sizeof(struct mesh_stream)==128 && offsetof(struct mesh_stream,slots)==8,"M37");` at `rdma/mesh-call.h:80` | `rdma/mesh-call.c:219`, fixed completion address `:149`; GPU alias/literal value `swift/Mesh.swift:646`, `metal-microbench/mesh_layer.swift:310` | sampler completion | 0 — device stores constant `(1<<63)|(frameOrdinal+1)` to the compiled address. The existing cold retirement reader invokes reference cleanup; it does not submit a device command. Completion comes from the caller-selected sampler on rank 0 and the final local vocabulary producer on other ranks; no consumer exists on those peers. |
| <a id="M38"></a>M38 supplied sampler operands | Rank 0 only: `Sarg_rq+width_q*b`; bias uses `Sarg_bias+4*(b*V+v)`; top-k output uses `Sarg_topk+4*(50*b+k)` | Bias `4*B*V`; temperature/min-p/seed/step/active/top-k/sample/LP `4*B` each; capture `B`; top-k IDs/LP `4*B*50` each. `_Static_assert(sizeof(uint32_t)==4 && sizeof(float)==4 && sizeof(uint8_t)==1,"M38");` at `rdma/mesh-call.h:86`. | Existing caller buffers `metal-microbench/bootstrap.swift:385`, `:392`, `:400`; retained/compiled bindings `metal-microbench/mesh_layer.swift:248`, `:282`, `:294` | consumer read | 0 — supplied numerical operands. Temperature, seed, active rows, token choice and top-k indices are numerical values, not collective descriptors. Native and resident entries share the sampling arithmetic. |

| <a id="M39"></a>M39 native resident command | One queue and one command prepared at start; dispatch index `3*s+kind`, kind embedding/sampling/body | Driver-owned encoding; native handle is 8 bytes, asserted by `rdma/mesh-call.h:91`. Three dispatches per slot with fixed buffers, offsets and grids. | `metal-microbench/mesh_layer.swift:421`, after all resident pipelines are constructed | consumer read | 0 — bind and commit once at setup; no ICB, replay, successor queue, timer or runtime re-encode. Resources remain retained through command completion. |

| <a id="M40"></a>M40 resident shutdown word | `R_r+s*S_control+a_stop(r)`, `a_stop(r)=32*ceil((a_scope(r)+8*U)/32)` | 32-byte aligned cell: atomic shutdown +0, reserved +4..31; `_Static_assert(sizeof(struct mesh_resident_control)==32 && offsetof(struct mesh_resident_control,shutdown)==0,"M40");`; matching MSL `static_assert`. | Canonical extent `metal-microbench/mesh_layer.swift:533`, binding `:35`; C assertion `rdma/mesh-call.h:97`; MSL assertion `metal-microbench/kernels.swift:88` | consumer read | 0 — each stage has a direct pointer to the same shutdown word. Only explicit deinit sets it. No lease generation, entry publication, cross-bank observation or restart catch-up. The single-command lifetime probe survived 120.101440 seconds and retired explicitly; this is a tested duration, not an unlimited-lifetime claim. |

| <a id="M41"></a>M41 prepared invocation publication | `Submit_r+128*s` | `128`, aligned 128: eight range addresses, eight range counts, instance address, next generation, stride and range count. `_Static_assert(sizeof(struct mesh_submission)==128 && _Alignof(struct mesh_submission)==128,"M41");` | `rdma/mesh-call.c:85`, `:87`, `:336`; retained by `swift/Mesh.swift:608`. | submit | 0 — one line supplies every destination. Each range receives generation+1 at stride 16; call indices are already in M18. No event-index list, group lookup, modulo, admission or result reset. |
| <a id="M42"></a>M42 invocation result and cold reference count | `Instance_r+32*s` | `32`, aligned 32: status snapshot, remaining references, invocation, initial reference count, reserved. `_Static_assert(sizeof(struct mesh_instance)==32 && _Alignof(struct mesh_instance)==32,"mesh_instance");` | `rdma/mesh-call.c:91`, reference totals in `mesh_calls_prepare`; result address retained by `swift/Mesh.swift:608` at setup. | result | 0 — result reads its directly bound status snapshot. Independent prepared invocation handles have disjoint backing. The result closes local references; reuse across ranks must follow the compiled graph lifetime, not an implicit admission check. The engine retains one handle for its one outstanding numerical evaluation; the chain clients bind all configured handles at setup. Link/function failures remain result values. |

| <a id="M43"></a>M43 contiguous CPU operand | `Operand_rf+32*(s*(input_count_f+output_count_f)+i)` | `32`: data +0, bytes +8, index/row +16/+20, generation address +24. Swift-importable ABI alignment 16; actual arrays use aligned_alloc(32), stride 32; sizeof/alignment/offset assertions in mesh-call.h. | `rdma/mesh-call.c:303` allocates the aligned array; `:317`–`:318` binds actual registered contiguous backing. | consumer read | 0 — the supplied numerical function receives the operand array. No page lookup, division by transport quantum, publication pointer or raw page API remains. Generation is a numerical input; its direct M20 address is bound at setup. |

| Fixed native binding | Start-time contents |
| --- | --- |
| M06 | `wr_id=(s<<32)+source_row(r,s,l)` (the row is fixed at setup); `next=M06(h(j)+k+1)` within the partial, NULL at its last fragment; `sg_list=M07(j)`; `num_sge=1`; opcode `SEND`; only the final request of each partial signals TX completion; flags fixed at setup. |
| M07 | Source `NIC_r(A_rl+s*S(r,l)+Q*k)`; length `min(Q,b(r,l)-Q*k)`; registration key resolved at setup. |
| M08 | `wr_id=j_rx`; `next=NULL`; `sg_list=M09(j_rx)`; `num_sge=1`; post entry and QP fixed at setup. |
| M09 | Destination `NIC_r(U_rlp+s*S(p,l)+Q*k)`; length `min(Q,b(p,l)-Q*k)`; registration key resolved at setup. |
| Native queue | One native QP per directed partial and invocation slot. QPs are assigned at setup: lower-rank outgoing publications first, then higher-rank outgoing publications. Each side already knows the total from its send/receive graph; exchanged publication indices bind matching QPs. The requested native queue depth is the largest fragment-chain extent on that link, computed at setup, rather than the former unconditional 4095 requests per queue. Actual granted depths are reported during native creation. RECVs for that partial are posted in increasing fragment order. TX probes every publication independently and posts its complete native chain when ready. No unrelated partial can hold its place in a receive queue. Native refusal concludes the affected execution; no hot retry. |
| M13 | FFN publication records are consumed only at setup (`metal-microbench/mesh_layer.swift:60`), producing constant offsets and store operands in the resident kernel. Vocabulary also compiles direct stores. CPU publication retains one record per partial TX cell, local availability word, or CPU notification. TX: destination=M04.ready, value=1, scale=0. Native QP/request pointers are written only by bridge setup, never by producers. Local availability: value=1, scale=1. CPU notification: destination=M18+16*a+8, value=1, scale=1; M18 already contains the call integer and invocation mask. Function constants fix record counts; no header, position, mask or reservation. |
| Full operand availability | M10 at `Z_r+32*j_rx(r,s,l,p,c(p,l)-1)+24`; earlier native chunks are in the same ordered native receive sequence. Each completion increments its own M10 by F without a last-chunk branch. |
| Reuse | Each layer has distinct backing. A slot's next decode step uses its next input token, produced after that slot's previous vocabulary consumption. No receipt-time storage selection or occupancy word. |

| T1: value ready → SEND posted | Instruction | Object |
| --- | --- | --- |
| T1.01 | Producer’s final visible payload store at its bound output address. | M01 |
| T1.02 | Producer stores 1 into each compiled partial-publication ready word. | M04 |
| T1.03 | TX acquire-loads ready at `T_rp+32*j`; its register scan advances regardless of readiness. | M04 |
| T1.04 | TX clears the accepted ready word before calling the native provider. | M04 |
| T1.05 | Load native QP and WR head from the same M04 line. | M04 |
| T1.06 | Invoke register-held native post with those operands and stack M15 output. | M06, M07, M15 |

| T2: RECV completion → consumer first read | Instruction | Object |
| --- | --- | --- |
| T2.01 | Load native completion status at `CQ_output_r+48*p+8`. | M11 |
| T2.02 | Load completed `wr_id` into `j_rx` at `CQ_output_r+48*p`. | M11 |
| T2.03 | Load previous generation at `Z_r+32*j_rx+24`; add F held in a register. | M10 |
| T2.04 | Release-store that generation to the same address. No intermediate stack reload or binding lookup. | M10 |
| T2.05 | FFN consumer loads generation at each compiled peer stamp address; used-peer flags and selection remain in registers. Vocabulary consumers also use compiled stamp/payload bindings. | M10 |
| T2.06 | Consumer loads first payload element from its prebound M02 address. | M02 |

| RX continuation after T2.04; independent of consumer's T2.05–06 | Instruction | Object |
| --- | --- | --- |
| T2.07 | Load inline publication count at `Y_rp+S_recv*j_rx+52`. | M08 |
| T2.08 | Load destination and multiplier together at `Y_rp+S_recv*j_rx+64+32*a`. | M13 |
| T2.09 | Select generation or 1 in registers and release-store to that destination. No range-array base, stack reload or native repost intervenes. | M04 or M18 |
| T2.10 | Load native post entry and QP at `Y_rp+S_recv*j_rx+32`; invoke with WR at `Y_rp+S_recv*j_rx` and bad-WR output at `RFP_rp-88`. | M08, M09, M25 |
| T2.11 | Load retirement row at `Y_rp+S_recv*j_rx+48`; cold buffer/frame release follows. | M08 |
| T2.12 | On polling backedge load link lifecycle word; empty TX probe also reads this word. | M17 |

| CPU completion → publication; emitted AArch64 | Instruction | Object |
| --- | --- | --- |
| P.01 | Load inline instruction count at call+88. | M20 |
| P.02 | Load invocation at call+36. | M20 |
| P.03 | Load destination and multiplier together at call+128+32*a. | M13 |
| P.04 | Multiply/add in registers, release-store `1+invocation*multiplier` to destination, advance 32 bytes. | M04/M10/M18 |
| P.05 | Tail-branch to cold cleanup after all stores; no publication frame. | M20 |
| C.01 | Load direct contiguous data pointer from operand+0, then numerical payload at its supplied offset. | M43, M01/M02/M03 |

| T3: prepared step boundary | Instruction | Object |
| --- | --- | --- |
| T3.01 | Producer stores the canonical token/input operands; existing input-preparation call publishes their generation. | M01/M02, M10 |
| T3.02 | Resident embedding loads its fixed input generation, then the token and embedding values. No numerical-worker submit or command construction intervenes. | M10, M26, M03 |
| T3.03 | Caller-selected rank-0 sampling threadgroup stores sampled outputs and their system-visible words; peers finish at their last local vocabulary tile. | M38 |
| T3.04 | Store completed generation at `R_r+s*S_control+a_sample(r)+32*(B-1)+24`. | M35 |
| T3.05 | Store the prepared completion event at `End_rs+8`. | M37 |

| Boundary | State |
| --- | --- |
| Decode command construction | Zero per-step allocations, encodes, commits or completion-block attachments. TensorFunction.resident only runs its setup body. |
| Input preparation | CPU input copy/derivation remains an existing numerical caller; F cells, plus optional traces. It is separate from producer-partial to resident-consumer handoffs. |
| Cold lifetime work | M37 feeds existing refcount/frame release. Transport retirement and prefill reduce-scatter remain. |
| Native generic Metal | One ordinary nonresident/prefill command allocation at invocation; no stored commands array or retirement rearm callback. Decode uses only the separate lifetime command and never calls this adapter. |
| Resident lifetime | One setup-time command outlives token generations. Explicit shutdown is the only termination request. Renewal, its environment switch, two-bank storage, ICBs, timers, entry flags and generation comparisons are deleted. |

| T3 host publication, before the resident continuation above; emitted AArch64 | Instruction | Object |
| --- | --- | --- |
| T3.P1 | `ldp w9,w8,[x1,#108]`: stride and root destination count. | M41 |
| T3.P2 | `ldr w0,[x1,#104]`: next generation. | M41 |
| T3.P3 | `str w9,[x1,#104]`: next generation plus stride. | M41 |
| T3.P4 | `ldr x9,[x1,#96]`: fixed invocation destination. | M41 |
| T3.P5 | `str w0,[x9,#20]`: submitted generation; no result reset. | M42 |
| T3.P6 | `ldr w12,[x10,x9,lsl #2]`: root range extent from the same M41 line. | M41 |
| T3.P7 | `ldr x13,[x1,x9,lsl #3]`: root range address from the same M41 line; add stamp offset 8. | M41 |
| T3.P8 | `stlr x11,[x13]`: generation+1; advance 16 bytes and decrement register range count. No per-consumer metadata read. | M18 |
| T3.P9 | Return submitted generation; no stack frame or native call. | register |
| T3.R1 | `ldp x8,x9,[x3]`; `dmb ishld`: acquire the directly held result snapshot. Success and pending return without a frame. | M42 |
