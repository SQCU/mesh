# State-rate policy learning

The current data contract, source dimensions, shared IR, output projections,
losses, native integration and execution limits are specified together in
[POLICY-PROGRAM.md](POLICY-PROGRAM.md). The controlling user quotes are in
[SPECIFICATION.md](SPECIFICATION.md).

Earlier measurement artifacts describe their recorded architectures. They do not
override the source contract or establish the current implementation. Repository
tests and verification harnesses were removed at the user's instruction.
