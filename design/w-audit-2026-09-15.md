# W audit — withholding work, storage and execution flow

This audit follows the operator correction in [deliverables.md](deliverables.md):
I18 prohibits guards and blocks that prevent values from being drained or
transacted. Dedicated queue polling, indexed page placement, native completion
handling and the declared dependency countdown are permitted. A conditional or
page-table load is not by itself evidence of an operation wait.

The former blanket polling/indexing prohibition and its proposed immutable-address
replacement are superseded. The indexed-address correction applies to mesh `b1703a3` plus native error
normalization `7c7fee4`. `b850d53` (ABI 48) subsequently replaces publication notices as
described below. ABI 49 removes the reclamation stack and collector thread.
W1's source path is repaired; W2–W6 remain open for the reasons below. No runtime
speedup is established by either replacement.

<a id="w1"></a>

## W1 — publish path

`mesh_call_complete` publishes declared output rows, then releases completed uses.
`mesh_publish` updates presence and walks fixed numerical/link use bitmasks. Those
are indexed dataflow operations:

- ABI 50 release-stores the row's presence word, replacing the packed presence
  read-modify-write. Notification bits retain their separate queue role.
- `mesh_notice_push` now performs one release OR per realized bitmap level.
  Readers exchange and enumerate indicated words; there is no CAS retry, reserved
  queue position or linked entry. The [proof](pages-and-functions.md#publication-notifications)
  covers simultaneous publishers and delayed summary updates.
- `mesh_buffer_release` publishes one free-pool bit when its decrement observes
  one. It performs no queue claim, retry, backing traversal or reader query.

Publishing a value waits for neither delivery, a consumer, nor another publisher.
Refcount zero is an ownership event, not a caller free protocol. X5/N1's instance
rings and rearming remain required independently of this repaired publish path.

<a id="w2"></a>

## W2 — receive path

`link_progress` owns one thread per link and direction. `mesh_progress` polls one
native completion per queue and attempts another receive before publishing that
completion. Native refusal leaves the post cursor for the next progress pass;
there is no software outstanding-frame admission gate. A native error concludes
the affected invocation out of band. The sticky receive-failure guard is removed.

Setup allocates distinct receive runs for each queue. The completion identifies
the physical page; the section definition in the tag selects that queue's prepared
binding. One indexed store associates the logical row with the
actual backing. Free rows are unmapped; receive pools own unfilled backing.
Disjoint queue runs keep RX threads from editing the same placement entries. ABI 53 reposts
actual backing on final reference release. Logical rows return to their binding's
RX-owned stack. Protocol 57 directly indexes active source heads and chunk ordinals.
Only free rows and pages can change owners; a live reader's backing is preserved.
In working ABI 55, each queue poll handles one returned section's page indices,
returns its logical row, and attempts an available post. The return-cursor queue
is removed: no page from a partly detached section is reposted before its row
returns. This is O(K) metadata work for K chunks, not a constant-time RX claim.
See [block addressing](pages-and-functions.md#block-addressing).

The operand binding now requests contiguous placement only for declared native
view factories and Core ML predictions. Raw functions receive the canonical
mapping and perform logical scalar indexing through `mesh_operand.load` without
an auxiliary placement output. The address helper has no readiness observation,
allocation, retry or presence wait. Consumer ownership protects its page entries
through native completion and output return. This changes neither the unfinished
N1 receive-row bound nor X9's missing resident GPU interface.

Numerical dispatch uses the published descriptor's section definition to select
the compiled consumer range. Setup stores each function/input relation once,
rather than once per resident row; actual row and invocation identity still flow
into the call's operand array. This adds a definition-word read to dispatch and
removes the duplicated target records, with no readiness branch or transport
change. The buffer descriptor uses an existing alignment gap, retaining 48 bytes.
RX allocation and forwarding still use their existing finite row ranges.

Protocol 57 replaces cycling source-chunk matching with an explicit definition
and chunk ordinal in the wire tag. RX indexes the peer-qualified binding and
active source head directly, then stores the actual backing at the logical
chunk offset. Prefix maps, per-chunk target records and occurrence cursors are
gone. The tag fields remain identical across concurrent senders of one backing.
This neither changes the dedicated progress loops nor resolves the separate
destination-row stack bound. This change reduces the committed library from 2,640 to 2,621 maintained
Swift/C/header lines (pending control-event work is separate); documentation is separate and no performance is claimed.

These mappings are required mechanisms, not violations. ABI 51 adds the X3
contiguous-operand path for chunk interleaving within a queue and independent
peer queues. ABI 53's [return cycle](pages-and-functions.md#receive-storage-return)
implements N1t's transport reuse without an operation wait, occupied-slot choice
or reader query. W2 remains qualified by N1's unfinished unbounded admission and
its live-value bound. Striping one logical section across multiple links remains T3 work.

### X3a receive matching prerequisite

If receives are posted at a then b, B may arrive first at a. The page table then
names a as B's backing. A arriving at b names b as A's backing. Consumer identity
follows logical rows, not receive ordinal. Both producers may publish immediately;
there is no requirement for one native QP per independent value.

For multiple peers, source row identifiers are interpreted within each peer/link's
prepared transfer relation; equal source integers from different peers are not
the same value. Interleaving completions from the two neighbors of a ring does not
require a shared completion order. Every logical value retains its actual backing
through its own declared readers and transfers.

ABI 51 replaces the consecutive-chunk assumption. A ready send rotates after
each posted chunk. Multi-chunk contiguous inputs are placed in canonical storage
inside the consumer launch; dependencies and references remain on the received
logical rows. Metal uses a blit encoder followed by the supplied encoder in one
command buffer. CPU/Core ML placement uses the numerical worker. No layout work
runs on a TX/RX thread, no extra completion hop is inserted, and only the supplied
consumer's own inputs participate. [Storage and byte costs](pages-and-functions.md#indexed-receive-runs-and-contiguous-consumers)
are explicit; this materialization is not called zero-copy.

<a id="w3"></a>

## W3 — send path

`link_publications` enumerates notified row bits, uses a prepared row-to-send range
and appends the ready send indices. ABI 50 gives each queue a circular array sized
for its declared edges; one TX thread owns its head and tail. The [capacity proof](pages-and-functions.md#reusable-send-queue)
requires no fullness guard. `link_send_ready` resolves and posts the current chunk;
the final native completion resets its cursor before releasing the send reference.
ABI 52 carries invocation identity separately from the source row. ABI 53 recycles
receive targets and backing; forwarding therefore reuses the send edge for a new
invocation. ABI 54 adds native slot return; unbounded call/status matching and instance
admission remain N1 work.

Native queue refusal is the admission limit. Empty-queue observations do not wait
for a particular tensor. A pending request must not stop other links from draining.
ABI 51 rotates an unfinished edge to the ring tail after every accepted chunk.
Ready values share the queue without whole-section posting order. Native refusal
retains the current entry and cursor; other queues continue being polled and posted.

<a id="w4"></a>

## W4 — submit path

`mesh_calls_submit` publishes root rows through the indexed notification bits. It has
no operation wait or allocation, but still lacks N1's reusable instance allocation
and immediate busy result when the free pool is empty. The finite index range is
not an unbounded invocation API. X1 has removed the notification stack; X5/N1 must
provide completion-driven reuse without modulo selection of an occupied slot.

<a id="w5"></a>

## W5 — result and reclamation

`Mesh.result` performs one status load. Declared numerical and transfer references
prevent success before those uses finish. There is no link scan in this path.

ABI 49 replaces the guarded stack with an indexed section free pool. Final release
sets one bit; setup consumes free entries only when its first allocation pass has
no suitable range, then makes one further pass. No numerical or transport path
performs that allocation work. Abandoned positive counts are discharged by the
bridge at the retirement event outside active link execution, following QP
teardown. There is no collector thread or deferred-reader retry list.
ABI 53 directs received sections' final-reference events to their owning RX
thread. Posted receive-pool pages never enter the general allocator while the
link is active. Device-close retirement returns that pool, including unused rows.

The [lifetime derivation](pages-and-functions.md#reclamation-events) states the
ordinary-completion proof and failure limits. This section pool is not the X5
per-worker SPSC instance pool. The arena bound, instance admission and R2 failure cancellation still need
implementation; ABI 54 supplies native slot return. N1 cannot reuse storage
just because its submission index wraps around.

<a id="w6"></a>

## W6 — invocation path

`mesh_call_progress` drains notices and walks prepared consumer ranges, decrementing
pending counts. Zero submits the supplied function. It never scans functions for
input presence. ABI 48 removes the notice list: `mesh_notice_take` enumerates only
pending bits using reader-local arrays, returning on an empty root.

ABI 52 removes `mesh_call_submit` and its consumer-slot-derived remote refresh.
Each publication names a logical invocation and actual input row; the function's
single worker matches that invocation's join record and independently assigns
its prepared native storage only when the operand countdown reaches zero. Working
ABI 55 uses a preallocated, linearly probed match table and owner-managed free
rings; matching therefore has metadata-probe cost but no cross-worker claim.
`mesh_call_input` resolves that actual row through the canonical page table, or
retains the prepared contiguous view for an input with placement. `MeshBindings`
selects the native view using the operand's own storage/page index. Copies use
the original source row. Final output ownership releases that same input row and
returns the native slot. Shared input arrival updates its declared finite set of uses;
no call can have launched before receiving that required dependency. The
[derivation](pages-and-functions.md#invocation-identity-and-storage-reuse) includes
opposite producer-slot orders and the unchanged finite-lifecycle limitation.
Core ML's feature-provider protocol requests values by name; its lookup is native
interoperability to minimize, not a reason to invent a second model interpreter.

ABI 51 binds dependency inputs separately from prepared operand views and retains
placement storage as auxiliary outputs through backend completion. ABI 53 re-arms
receive storage. Metal command buffers are replenished by their numerical worker after native completion and
final output ownership (ABI 54); completed objects are never recommitted.
Unbounded invocation matching and instance admission still require N1.

<a id="w7"></a>

## W7 — collective compositions

All collective verbs are declarations over sends and supplied functions. Explicit
owners, destinations, routes and combine functions are realized before execution.
T2 expands a route into sends of the same received section at each relay. No route
lookup or model interpretation occurs during transport. Collective semantics do
not depend on world size or arrival order. Existing runtime defects still apply
to the realized edges.

<a id="w8"></a>

## W8 — caller bindings

The Gram caller's supplied Accelerate operations return after producing their
outputs; their library dispatch/publication paths inherit W1–W6. This is source
composition, not evidence of distributed speedup.

Engine `769567d` and `aad2874` added copied prompt storage, host-polled stamps and
an implicit `encodeWaitForEvent(prefillDone)` in embed. `74a62ff` removes them.
The I18 clarification does not permit that wait: prefill must publish operands
consumed by decode through declared dataflow. The remaining engine attention
binding still reaches the host `attentionSplits` scan. In `lm_engine.swift:1909`,
`encodeWaitForEvent(graph.meshStepEvent, ...)` also precedes `encodeDecodeOutput`.
That implicit output-stage wait remains an E3 defect: bind output processing as a
consumer of the Mesh-produced operands instead of deleting the wait while leaving
an undeclared read. W8 is partial pending those integrations.

The concurrently integrated public report
`metal-microbench/output_data/mesh_e3/compare_A_8x8.out` reports inferior two-node
decode at engine `772a8cb` / mesh `7c7fee4`. It is the historical decode-shaped run that I21 now excludes from legitimacy
measurements. E3 acceptance requires the E5 prefill path with E4 placement; the
old run must not be repeated as validation. The launch-granularity and host-staging explanations in rows E1c/19k/19l
are implementation work to investigate and fix, not a page-table impossibility.

## Implementation order

1. X1 dense presence/notifications and W1's final-reference publication are implemented in source;
   retain their single-publication/declared-ownership contract during reuse.
2. X5/N1: transport and native storage return are implemented; finish instance
   free-pool admission and replace finite call/status matching without occupied-slot
   selection. Preserve the realized live-value bound.
3. X3a/X3/T3: ABI 51 supplies per-queue interleaving and contiguous operand placement;
   integrate reusable matching and the separate multi-link striping requirement.
4. N2/E3: complete recovery and prefill-to-decode dataflow, then exercise the public
   chain using the existing measurement path. No documentation correction or
   compilation substitutes for the required speedup evidence.

ABI 48 validation: the C sources pass compiler diagnostics; the bridge, Mesh
module, existing Core ML/Gram/indexed/sync callers and engine Mesh target build.
The compiled publication path uses `ldsetl` and notification dequeue uses `swpa`;
the removed CAS loop is not recreated by those atomic operations in this build.
No runtime experiment or speedup claim accompanies this source change. Reclamation
has since been replaced in ABI 49: C diagnostics, bridge, existing callers and
engine Mesh integration build. No runtime experiment accompanies that change.

ABI 50: the bridge, Mesh module, existing Core ML/Gram/indexed/sync callers and
engine Mesh target build; C diagnostics and `git diff --check` pass. The compiled
`mesh_publish` uses `stlr` for the presence word. The source proof covers TX ring
capacity and cursor retirement; no repeated invocation, native stamp consumer,
GPU visibility, deployment or speedup has been demonstrated by this change.

ABI 51: C diagnostics, the bridge, Mesh module, existing Core ML/Gram/indexed/sync
callers and the engine Mesh integration build. Source review traces the original
receive rows through fixed chunk mappings to canonical input views and native
completion. `examples/gram-chain-ring.json` uses the existing eight-block chain
with four ranks and two physical neighbors each. No runtime or speedup claim
accompanies these changes. The separate N1/X5, X9, T3 and recovery work remains.

ABI 53: C diagnostics, bridge, Mesh module, existing Core ML/Gram/indexed/sync
callers and the engine Mesh integration build. Source review covers per-queue
return progress, the physical-page and logical-row capacity bounds, repeated
forwarding, and pool ownership through QP teardown. Maintained Swift/C/header
source is 2,272 → 2,349 lines; documentation changes are separate. N1t's receive
storage return is implemented within the realized finite capacity. Unbounded
call/status matching, native rearm and per-worker instance pools remain open.
No runtime, deployment or speedup claim accompanies this change.

ABI 54 adds [native slot return](pages-and-functions.md#native-slot-return),
including functions driven entirely by received operands. Native callbacks publish
outputs immediately and report retirement through the existing notice mechanism;
only numerical workers change their free-index arrays or replace Metal commands.
The finite call/status arrays and instance admission remain open at N1/X5.
Source is 2,349 → 2,406 lines; documentation changes are separate. C compiler
diagnostics, bridge, Mesh module, existing Core ML/Gram/indexed/sync callers,
and the engine Mesh integration build pass. No runtime or speedup is claimed.
