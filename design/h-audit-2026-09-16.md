# H audit — dependent loads and metadata lines per hot event, `6a8d422` (MESH_VERSION 75)

Read-only recount of the four hot flows of [group H](deliverables.md#h--hot-and-cold-data-flow) at `origin/main` `6a8d422` (43 commits past `60efd51`; `rdma/` churn 948+/732−). All line numbers are that blob.

**Method.** *depth* = longest chain of loads whose address needs a prior load's value, from the event trigger to the named effect. *lines* = distinct 128 B metadata lines touched (payload excluded; ring/stream line and hdr line 0 counted once). Baseline (`f1ae04a`) and `60efd51` figures are the earlier ontology audit's; the enumeration here is explicit so the method can be checked.

## Event 1 — value ready → `ibv_post_send`

Producer (worker or Metal completion thread), `mesh_call_complete` rdma/mesh-call.c:456

| # | load | file:line | dep | line |
|---|---|---|---|---|
| a | call{memory,operands,input_count,output_count,invocation} | mesh-call.c:457-459 | d0 (arg) | L1 call (64 B) |
| b | operands[i].row | :461 | d1 | L2 operand array |
| c | hdr{buffer_off,target_off,links,presence_off} | mesh.h:113,104,110 | d1 | L3 hdr line 0 |
| d | buffers[row].sends / .uses | mesh-dataflow.c:256,259 | d2 | L4 buffer |
| e | targets[row][i].stream,.index | :257 | d2 | L5 target array |
| f | stream.position,.mask | mesh.h:162 | d3 | L6 ring |
| g | **STORE slots[at]** | mesh.h:163 | d3 | — |

Cold, after the store: call→function (:462, d1), function→calls (:463, d2), workers[w].active (:464, d3), consumed[] (:466), operands[input].row (:467) → `buffer.references` fetch_sub (mesh-dataflow.c:183, contended), return-slot push (:185), `active` fetch_sub (:472). H5 holds on the producer.

TX spinner `mesh.rdma.send`, `link_publications` mesh-flow.c:358 → `link_send_ready` :291

| # | load | file:line | dep | line |
|---|---|---|---|---|
| h | link.notices{inputs,count,cursor} | mesh.h:167-168 | d0 | L7 link |
| i | inputs[cursor]{slots,position,mask} | :170 | d1 | L8 |
| j | ring slot (acquire) | :171 | d2 | L9 ring |
| k | send_edges[first].end,.queue,.chunks | mesh-flow.c:362-365 | d3 | L10 edge line 0 |
| l | link.ready[q].first,.mask,.tail | :364-365 | d4 | L11 link (ready[] array) |
| m | **STORE send_ready[]** (first hot store) | :365 | d4 | L12 |
| n | send_ready[head] reload | :294 | d5 | L12 |
| o | send_edges[pending.first]{span.addr,post,pair,request} | :295-299 | d6 | L10 + L13 (request at 48..176 crosses into edge line 1) |
| p | STORE tag→value (address from span.addr) | :297 | d7 | L14 payload tail |
| q | `ibv_post_send(&request)` | :299 | d6 (provider derefs sg_list → d7) | — |

| | baseline f1ae04a | 60efd51 | now 6a8d422 | H7 target |
|---|---|---|---|---|
| dependent depth publish→post | 8 | 10 | **9–10** (producer 3, spinner 6–7) | producer 0–1, spinner 1 |
| metadata lines | 8 | 12 | **14** (producer 6, spinner 8) | ≤1 producer beyond the call record, ≤1 spinner |
| contended atomics | | | presence[row], buffer.references, worker.active | none |
| H5 | | | producer yes; spinner **no**: `link_progress` :379-382 runs `mesh_progress` (SEND completions → refcount/instance fetch_subs, :341-346) before `link_publications` each iteration, so every doorbell waits behind the previous batch's cold release in program order | doorbell independent of cold backlog |

## Event 2 — RX completion → worker ring store + re-post

`mesh_progress(link,MESH_RECEIVE)` mesh-flow.c:307

| # | load | file:line | dep | line |
|---|---|---|---|---|
| a | link{M,provider,qps}; queues[q]{poll,completions} | :308-312 | d0/d1 | L1 link, L2 mesh_queue |
| b | poll → wc{status,wr_id} | :317-321 | d1 | L3 |
| c | receive[q]{region_base,page_shift,block_reciprocal,region_blocks,page_base,block,pool_offset,client_data,records,sends} | :320-330 | d1 | L4 (128 B, 64-aligned: may straddle) |
| d | tag→value | :322 | d2 | L5 payload tail |
| e | records[(u32)tag]{entry,row,last,completions,send_count,sends} | :327 | d3 | L6 |
| f | STORE entry→mapping, entry→address (page table) | :328-329 | d4 | L7 |
| g | sends[region·send_count+j] | :332 | d4 | L8 (+1 per 8 bindings) |
| h | STORE binding.target→addr/lkey (SGE in another link's send_edges) | :333 | d5 | L9.. (one per forwarding binding) |
| i | if record.last: hdr; buffers[row].sends; targets[row][i]; stream hdr; **STORE slot (fire)** | :336 → mesh-dataflow.c:253-260 | d1/d4/d4/d5/d5 | L10 hdr, L11 buffer, L12 targets, L13 ring |
| j | presence store, uses pushes, buffer.invocation store | :258-261 | | same lines |
| k | buffer.references fetch_sub (contended with worker :467/:471 and TX :344) → channel → FREE-plane fetch_or or return push | :337 → :183-185 | d4 | L11, L14/L15 |
| l | instances[i].remaining fetch_sub (stride-0 rows only) | :338 → mesh.h:92 | d4 | L16 |
| re-post | `link_receive` :350→:263: ready.head/tail :254; pages[head&mask] :256 (d1, L17); requests[page]{span,request} :256 (d2, L18); queue.receive → `ibv_post_recv`; then returns reader :272 (L19,L20) and per returned row buffer :275, page entries[chunk].mapping load+store :278-279 (d3, L22), instances[frame+i] :284-286 (L23) | | | |

The re-post is not the completed page: it re-arms pages the consumer already returned via the returns stream, after the whole completion batch and its refcount/instance work; the just-completed page is re-armed only when its row returns (:272-281). H3's "re-post immediately; the frame ring guarantees the reader finished" is not implemented.

| | baseline | 60efd51 | now | H7 target |
|---|---|---|---|---|
| depth poll→fire store | | | **5** (wc→tag→record→publication triple→stream) | 1 (`receive_record[wr_id].w`) |
| lines to fire store | | | **13** + send_count SGE lines | ≤1 |
| depth poll→re-post | | | 2 from the ready ring, gated on a return event | 1, same completion |
| H5 | | | **fails**: page-table stores (f) and binding loads/stores (g,h) precede the fire store | |
| composite poll→consumer first byte | 14 / ≈25 | 21 / ≈43 | **≈13 / ≈23–25** (5 + event-3 launch 6 + operand→page entry→data 2) | ≤2 loads, ≤4 lines |

## Event 3 — worker ring read → supplied-function launch

`mesh_call_progress` mesh-call.c:146

| # | load | file:line | dep | line |
|---|---|---|---|---|
| a | calls.running acquire (per iteration) | :153,155 | d0 | L9 calls |
| b | worker.arrivals{inputs,count,cursor} | mesh.h:167-168 | d0 | L1 worker (128 B) |
| c | inputs[cursor]{slots,position,mask} — round-robins up to P streams per take | :170 | d1 | L2 |
| d | slot; STORE 0; position++ | :171-174 | d2 | L3 ring |
| e | targets[i].end; whole `mesh_use` (64 B) | mesh-call.c:157-158 | d3 | L4 |
| f | call.invocation RMW; --call.pending; call.index | :159-160,162 | d4 | L5 call |
| g | worker.active fetch_add (contended with :472) | :161 | d0 | L1 |
| h | use.submit → Swift trampoline: MeshInvocation{submit,inputCount,outputCount} | Mesh.swift:394-397 | d4 | L6 |
| i | submit closure context | :290/:295 | d5 | L7 |
| j | .metal: commands[index] → encode → **supplied function** | :256-258 | d6 | L8 |
| copies | placements[inputs[slot].index] (d5-6), copy.source→mapping (d7), views[...] (d8), blit | :245-247 | | +3 |
| first byte | use.operands → operand[k].pages (d4) → pages→address (d5, page-table read) → data (d6) | mesh-call.h:16, Mesh.swift:10,17 | | +2 |

| | now | H7 target |
|---|---|---|
| depth ring read→launch | **6** | 1 beyond the ring |
| lines | **8–9** (+P·2 probe lines when P>1) | ≤3 |
| H4 conditionals | ring empty, `--pending==0`, plus `calls.running` and the `invocation_mask` select | ring empty, pending==0 |
| H5 | `worker.active` fetch_add (:161) precedes the launch | |

## Event 4 — send completion → release

`mesh_progress(link,MESH_SEND)` mesh-flow.c:340-347

| # | load | file:line | dep | line |
|---|---|---|---|---|
| a | queues[q].poll → wc.wr_id | :312,317 | d1 | L1, L2 |
| b | send_edges[wr_id]{remaining,chunks,row,instances,completions} (offset ≥176 → edge line 1) | :341-345 | d2 | L3 |
| c | --remaining RMW | :342 | d2 | L3 |
| d | hdr buffer_off; buffers[row].references fetch_sub acq_rel (contended 3 ways) | :344 → mesh-dataflow.c:183 | d3 | L4, L5 |
| e | channel → FREE plane fetch_or (contended with allocator) or return-stream push | :184-185 | d3/d4 | L6 / L7 |
| f | instances[i].remaining fetch_sub; on last: status CAS (16 B), available store | :345 → mesh.h:89-96 | d3 | L8 |

| | now | H7 target |
|---|---|---|
| depth | **3–4** | 1 (`free_ring.push(send_record[wr_id].release_row)`) |
| lines | **5–8** | ≤1 |
| H5 | **fails**: no free_ring; refcounts, plane bitmap and instance counters are decremented on the TX spinner before its next `ibv_post_send` | |

## H8 deletion checklist

| structure | status at 6a8d422 |
|---|---|
| `mesh_wire_tag` beyond row/frame | **present**: one 8 B word `invocation<<32|record index` (mesh.h:61; written mesh-flow.c:297; read :322). The WR does not carry it (recv wr_id = span.addr, send wr_id = edge index), so the tag is the sole carrier — a dependent payload-line load per RX completion |
| `buffer.definition` | deleted → per-row `mesh_target[]` at target_off + `buffer.sends/uses` |
| `buffer.invocation` | **present** mesh.h:27; written mesh-dataflow.c:261, runtime reader mesh-flow.c:285 |
| `buffer.mapping` / `mesh_buffer_pages()` | renamed → `mesh_page_entry{mapping,address}` page table (mesh.h:33). Runtime readers: mesh-flow.c:278 (RX return), mesh-call.h:16-25 via Mesh.swift:10,17,100,246,291 |
| `receive.definitions[]` | deleted → `receive->records[]` (mesh-flow.c:26-31; reader :327), indexed by the tag word, not wr_id |
| `receive_binding.rows[]` | deleted → `mesh_receive_record.row` |
| `receive.active[]` | replaced by `receive->pages[]` ready ring (mesh-flow.c:36,256,278) — pages re-armed in *return* order (runtime permutation) |
| notice levels / `mesh_notice_reader` | deleted → `mesh_event_reader` over `mesh_stream` slots (mesh.h:49-50,166-178), but a reader is a list of P streams probed round-robin, not one ring |
| `mesh_use` + `program[]` | `program[]` deleted → `worker->targets[]`; **`mesh_use` present** (mesh-call.c:32-39, 64 B), runtime reader :157-162 |
| `mesh_join`/`matches`/`join_free`/`join_rows`/`mesh_hash`/`mesh_call_match` | deleted → `function->values[frame]` (mesh-call.c:113,126) |
| per-function `available[]`/`values[slot]` | `values` indexed by frame; `available` → `mesh_instance.available`, read only at submit (mesh-call.c:438) — acceptable |
| `mesh_operand.{row,invocation,pages,…}` + `mesh_operand_address` | **present**: mesh-call.h:9-21 (40 B); `mesh_operand_address` runtime reader Mesh.swift:17; `mesh_operand_data` Swift:10,291 |
| `rearm` allocation | **present**: mesh-call.h:28; Mesh.swift:255 `queue.makeCommandBuffer()` per completion, invoked mesh-call.c:173 on the worker |
| Swift `copies/chunks/sources/targets` | **present**: Mesh.swift:201-252 (placements, blit :245-248), CPU memcpy :282-292, built in `bind` :370-381 |

Deleted or acceptably replaced: 6 of 14. Present with a runtime reader: 8.

## Runtime structs

| struct | size/align | assert |
|---|---|---|
| mesh_buffer | 64/64 | mesh.h:30 |
| mesh_page_entry | 16/16 | :34 |
| mesh_stream | 128/128 | :41 |
| mesh_target | 16/8 | size only :43 |
| mesh_events | 128 hdr | :48 |
| mesh_event_input / reader | 16 / 16 | :51-52 |
| mesh_status / mesh_instance | 16 / 32/32 | :58,:60 |
| mesh_wire_tag | 8 | :62 |
| mesh_transfer, mesh_pool, mesh_link_info (~192), hdr | 32, 16, —, — | none |
| mesh_operand | 40/8 | size only mesh-call.h:14 |
| mesh_call | 64/64 | mesh-call.c:16 |
| mesh_use | 64/64 | :39 |
| mesh_call_worker | 128/128 | :47 |
| mesh_send_edge | 256/128 | mesh-flow.c:16 — WR 48..176, release fields ≥176 → 2 lines per post, 1 per completion |
| mesh_receive_request | 64/64 | :18 |
| mesh_send_range / mesh_send_binding | 16/16 | :22,:25 |
| mesh_receive_record | 32/32 | :31 — the only hot record meeting H2's ≤32 B |
| mesh_receive | 128/64 | :42 — 64-aligned, may straddle |
| mesh_queue | 64/64 | mesh-verbs.h:39 |

## Working tree at audit time (81+/56−, later committed as `94bcedb`, ABI 76)

`struct mesh_publication {sends, uses, row; mesh_target targets[]}` per row at `target_off`; `mesh_publish` takes the pointer; `mesh_operand.row` → `publication` pointer (40→48 B); `mesh_call_complete` split into a hot body plus a `noinline` musttail `mesh_call_cleanup`; `mesh_receive_record` replaces row/last with the pointer. Net on the counted paths: event 1 producer 6→5 lines, depth unchanged (3); event 2 to the fire store 13→12 lines, depth unchanged (5); events 3 and 4 untouched.

## Follow-up — direct first SEND and deletion of contribution typing

The original count above remains the baseline. The subsequent source change in
`link_publications` removes the unconditional pending-range enqueue/reload before
a new publication's first native post. The TX thread holds the native-request
array base across iterations. From the event slot, the request index addresses
that array directly; queue selection no longer determines a second record lookup
before the post. Optimized ARM64 loads the prepared request header and issues
`blr` before touching the pending-range queue.

| first-post path | baseline above | changed source |
|---|---|---|
| dependency chain after reading the event | edge → queue state → pending range → edge | edge |
| TX reader through first native post, same counting convention | 6–7 dependent loads / 8 metadata lines | 3–4 dependent loads / 6 metadata lines |
| producer plus TX, including ABI 76's publication layout | 9–10 dependent loads / 13 metadata lines | 6–7 dependent loads / 11 metadata lines |

The range queue still schedules unfinished requests and native-capacity refusals;
it is not deleted globally. Its state and reload are removed from the first-post
dependency chain. Native request/provider loads, stream probing, completion
cleanup and subsequent queued posts remain. No H row passes on this evidence.
The separate TX/RX entry points also delete the worker-to-link wrapper and runtime
direction selection, with no replacement role record.

The Swift contribution-type feature is deleted rather than moved: no partial
flag, registry, rejection closure, privileged function binder, or completed-value
identity replacement remains. Reduction's supplied function uses public `call`.
Those are setup deletions and contribute **zero** to the runtime depth reduction
reported above. The native-call, receive-placement and ownership paths remain
subject to the original audit. No runtime workload was run.

## Follow-up — native function dispatch

Relative to `3bd1eaa`, the worker no longer traverses `MeshInvocation` or enters
its C callback adapter. Setup retains concrete Swift function values in the
existing memory owner and gives C their code/context pairs. Clang `swiftcall`
with `swift_context` supplies the native register convention. The generated
launch sequence loads `{code, context}` from the use record, loads dependency
and operand fields from the call record, then invokes the prepared code. The
Swift entry loads its actual captures directly. No generic function conversion
or block conversion lies between them.

| mechanism | before | after |
|---|---|---|
| use record | 64 bytes, including duplicate operand pointer/count | 32 bytes, asserted size/alignment |
| invocation object | one heap object read before native launch | deleted; setup ownership array has no dispatch reader |
| callback adapter | C callback unwraps object, then calls stored Swift closure | direct Swift ABI invocation |
| lifetime RMW before launch | shared `active` increment | deleted; local submitted count changes after launch returns |
| arrival admission | second `running` acquire/branch | deleted |
| native completion accounting | shared decrement after publication/cleanup | one shared completion increment in the same position |

The already-required call record supplies its operand pointer/count; removing
the copies does not introduce another record on that dependency chain. The
wrapper record and its adapter stack frame are deleted. Compiler-generated
backend closure work remains. A complete revised end-to-end depth count is not
claimed from this local deletion, and the original baseline above is retained
as historical evidence.

The engine’s decode/prefill Mesh calls now include output normalization,
projection, sampling and logprob extraction in their final command buffers.
The separate output command buffer, Mesh shared-event signal/wait pair and
serial value are removed. Host ticks observe completion without preventing
native output work from starting. Local standalone execution still owns its
ordinary synchronous command-buffer API. Session-page release follows ARC,
with failed submitted work retained by the existing graph state until teardown.
This does not implement R2 cancellation or repair the engine’s other shared
storage and contiguous-copy limitations.

The remaining H defects are still real: O(P) stream probing, receive mapping and
forwarding updates, contiguous input copies, native rearm allocation and inline
transport reference/instance bookkeeping. No H row becomes complete merely
because the dispatch subpath improved. Builds and optimized assembly are the
evidence; no runtime workload or performance result is claimed.

## Verdict at the original baseline

Not converging on H. Event 1 has gone 8/8 → 10/12 → 9–10/14 (depth/lines): each memoization commit adds a record on the chain (send_ready range ring, send-binding array, receive geometry record, second edge line for the 128 B WR) instead of removing one; depth flat, lines rising. The composite poll→first-byte chain came down from the `60efd51` peak (21/≈43) to ≈13/≈24, which only recovers the baseline (14/≈25): the recent commits mostly undid reconstruction the same session introduced. Against H7 none of the four events passes. Structural blockers are the H8 items still present with runtime readers: the row→buffer→targets→stream publication chain instead of a start()-time index range; the tag-indexed receive record plus page-table stores and forwarding-SGE rewrites between completion and fire; `mesh_use`→`mesh_call`→Swift closure→command buffer to launch; the `mesh_operand`/page-entry deref at the consumer; `rearm`; the Swift placement copy; inline refcount/instance release on the spinners. Struct asserts (20 structs) are the one rubric met.
